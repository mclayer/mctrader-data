"""mctrader_data.nas_metrics — NAS MinIO Prometheus metric exporters.

Stage 2 신규 패키지 (MCT-150).
주의: mctrader_data.metrics (기존 metrics.py) 와 다른 패키지 — NAS 전용 metric namespace.

Metric prefix: nas_uploader_* (MCT-151 nas_invariant_* prefix-disjoint, NFR-4 freeze).
"""
