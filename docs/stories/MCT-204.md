---
story_key: MCT-204
story_scope: data
story_issues:
  - repo: mclayer/mctrader-data
    number: 184
    url: https://github.com/mclayer/mctrader-data/issues/184
status: phase:요구사항
parent_epic: "단독 Story (Epic 미생성 — _tick stall + historical L1 cleanup)"
created_at: 2026-05-19
delegates: []
adr_carrier: "mctrader-hub:docs/adr/ADR-027 + ADR-029 amendments (TBD)"
adr_status: TBD
upstream_story: []
downstream_story: []
related:
  - "#48 MCT-159 compactor L1 backlog cleanup"
cross_repo_isolation: cross-repo 1건 (mctrader-hub ADR-027 §D5/§D7 + ADR-029 D1=B amendments)
worktree: .claude/worktrees/mct-204
branch: mct-204-compactor-tick-stall-historical-l1-reclaim
cutoff_classification: "full-lane Story (강제 대상 — 아키텍처 변경 + 기존 ADR amendment)"
spec: docs/superpowers/specs/2026-05-19-compactor-tick-stall-historical-l1-reclaim.md
live_touching: false
deputy_spawn: "6 permanent + LiveOps OFF + LiveOrdering OFF (read-only forward fix + reclaim, ordering side effect 0)"
---

# MCT-204: compactor _tick stall 해소 + historical L1 회수 경로 신설

- **Issue**: [mclayer/mctrader-data#184](https://github.com/mclayer/mctrader-data/issues/184)
- **Spec**: [docs/superpowers/specs/2026-05-19-compactor-tick-stall-historical-l1-reclaim.md](../superpowers/specs/2026-05-19-compactor-tick-stall-historical-l1-reclaim.md)
- **ADR**: mctrader-hub:docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md (§D5/§D7 amendments, TBD) + mctrader-hub:docs/adr/ADR-029 (D1=B amendment, TBD)

## §1 사용자 요구사항

> 1. "이전에 compact 완료된 데이터는 삭제하라 했는데 여전히 해결되지 않아 계속 로컬 디스크가 낭비되고 있다."
> 2. "함께 L3로 데이터도 잘 쌓이고 있지 않다."

## §2 근본 원인 — single origin (RequirementsPLAgent + DomainAgent 통합)

두 사용자 호소는 **단일 origin**: forward `_run_l2` worker thread 가 NAS GET 안에서 stall → 같은 `_tick` 안 후속 step (`_run_l3`, `_cycle_count++`, `scan_and_cleanup_legacy`) 영원히 진입 못함.

### §2.1 sequential 차단 chain (runner.py)

| step | 위치 | 동작 | 차단 영향 |
|---|---|---|---|
| 1 | `runner.py:107-112` | `for sealed in sealed_list: self._l1.compact_segment(sealed)` | 정상 (L1 dispatch=1914) |
| 2 | `runner.py:114-116` | `await run_in_executor(None, self._run_l2)` | **blocking await** — worker stall 시 main thread 영원 대기 |
| 3 | `runner.py:118-120` | `await run_in_executor(None, self._run_l3)` | **2 차단 시 도달 0** (L3 dispatch=0, 사용자 호소 #2) |
| 4 | `runner.py:131` | `self._cycle_count += 1` | 2 차단 시 영원히 0 |
| 5 | `runner.py:132-145` | `if cycle_count % LEGACY_CLEANUP_EVERY_N_CYCLES == 0: scan_and_cleanup_legacy(...)` | 2 차단 시 호출 0 (사용자 호소 #1) |

### §2.2 worker stall 원인 — rglob outer iter 폭주

| 위치 | 코드 | 측정 결과 |
|---|---|---|
| `runner.py:158` | `(self._root / "market").rglob("*/tier=L1/**/part-*.parquet")` | **historical 16,918 file iter** (5/13~17 130GB 누적) |
| `runner.py:160-173` | 발견 partition × `[today, yesterday]` × 24 hour seen 중복 제거 | 400 unique (e,s,c) tuple × 48 dispatch = **19,200 worker NAS GET** |
| `runner.py:211` | `(self._root / "market").rglob("*/tier=L2/**/part-*.parquet")` | L3 동형 |

→ rglob outer iter 가 today/yesterday window 와 무관한 historical partition 까지 매번 scan. 평균 6 sec/dispatch × 19,200 ≈ 32h/cycle. py-spy 박제 (Thread 133 = `get_streaming` → `_compact_hour_nas` → `_run_l2_for_parquet`, idle 2h+).

### §2.3 historical L1 회수 경로 부재

| 측정 | 결과 |
|---|---|
| operator one-shot `scan_and_cleanup_legacy` (31 iter, batch 500) | L2 10.8GB 회수 / **L1 0 byte 회수** (iter 10+ 매 batch `cleaned=0 preserved=500`) |

`scan_and_cleanup_legacy` 의 회수 gate = L1/L2 양쪽 NAS HEAD 4-tuple verify pass. `promote_historical` 는 L1 → L2 변환 + L2 NAS PUT 만 수행 (`runner.py:471-473` 주석 `source_to_delete=None`, MCT-202 D-3 cascade 의도 보존). L1 NAS 객체 부재 → 4-HEAD verify fail → preserved.

→ **historical L1 130GB 회수 경로가 본질적으로 부재** (forward eager cascade = today/yesterday only, promote-historical = L2 만 PUT, sweep = NAS HEAD 의존). 신규 회수 경로 신설 필요.

### §2.4 ADR-027 §D5 incident pattern 정합

- ADR-027 §D5: "NAS PUT 4xx fail-fast — silent fallback 차단" (2026-05-18 amendment, mctrader-hub SSOT)
- 본 origin = **silent stall** (raise 없음, retry_queue 0 흡수, 사용자 인지 없음) — §D5 amendment 의도 (silent failure 차단) 확장 필요. ADR-027 §D5 amendment 추가 (cooperative scheduling head-of-line blocking pattern 박제).

## §3 scope / Change Plan

(ArchitectPLAgent lane write — TBD)

## §4 ChangeImpact + Feasibility + Continuity (RequirementsPLAgent 통합)

### §4.0 관련 코드 경로

| 영역 | 경로 | 본 Story 영향 |
|---|---|---|
| forward `_tick` orchestrator | `src/mctrader_data/compactor/runner.py:86-149` | Layer 2 (asyncio task 분리 + per-step timeout) |
| forward `_run_l2` | `src/mctrader_data/compactor/runner.py:151-175` | Layer 1 (rglob → `_discover_partitions_in_range` 재사용) |
| forward `_run_l3` | `src/mctrader_data/compactor/runner.py:204-227` | Layer 1 동형 |
| 기존 partition discovery helper | `src/mctrader_data/compactor/runner.py:421-457` | Layer 1 재사용 (재구현 0) |
| historical promote dispatch | `src/mctrader_data/compactor/runner.py:460-507` `_historical_dual_write` + `run_historical_promotion` | Layer 3 (batch 종료 hook 추가) |
| 신규 historical reclaim module | `src/mctrader_data/compactor/historical_reclaim.py` | Layer 3 (신규 helper +180 LoC) |
| CLI promote-historical | `src/mctrader_data/cli.py` | INV-B 게이트 (today/yesterday abort) |
| metric exporter | `src/mctrader_data/observability/metrics.py` | 신규 3 metric |
| operator script (deprecation 후보) | `_reclaim/incremental_l1_reclaim.py` | Layer 3 가 동형 GATE-2 pattern 흡수, Phase 2 후속 deprecate |

### §4.1 ChangeImpact (변경 델타)

| 컴포넌트 | 변경 종류 | LoC 예상 | Risk |
|---|---|---|---|
| `runner.py::_tick` | asyncio.create_task 분리 + per-task cadence + timeout | +120/-30 | MEDIUM (cycle_count / sentinel timing 의존성) |
| `runner.py::_run_l2/_run_l3` | rglob → `_discover_partitions_in_range` 호출 | +60/-40 | LOW (helper 기존 박제) |
| `historical_reclaim.py` (신규) | partition batch 종료 hook + 4-HEAD verify + sentinel `.l1-promoted` | +180 | MEDIUM (L2 NAS HEAD pattern 재사용) |
| `cli.py::promote-historical` | INV-B abort gate (date >= today-1) | +20/-5 | LOW |
| `metrics.py` | 3 신규 metric Counter/Gauge | +60 | LOW |
| tests (3 file 신설) | INV-A/B/C/D + AC-1~5 통합 | +610 | LOW |
| scripts/verify_historical_l1_reclaim.py | 별 verify (incremental_l1_reclaim.py 동형) | +150 | LOW |
| **합계** | | **~600-900** | sequential_single_story (PMOAgent Phase 2 판정) |

### §4.2 Feasibility (자연스러운 구현 가능성)

- **Layer 1 (rglob 축소)**: 기존 helper `_discover_partitions_in_range(root, channel, start_date, end_date)` 재사용 — re-implementation 0. Helper 가 production layout (`date=<d>/node=<id>/part-*.parquet`) 박제됨 (rglob 사용, c169720 CRITICAL fix 정합). **HIGH feasibility**.
- **Layer 2 (asyncio 분리)**: Python asyncio 표준 패턴 (`asyncio.create_task` + `asyncio.wait_for` timeout). 기존 `await run_in_executor` 단순 대체. cycle_count 의 sentinel cadence (`LEGACY_CLEANUP_EVERY_N_CYCLES`) 는 별 task 의 자기 cadence interval 로 대체 (env `MCTRADER_COMPACTOR_STEP_TIMEOUT_SECONDS` default 600s). **MEDIUM feasibility** (cycle_count timing regression risk → integration test 박제 의무).
- **Layer 3 (historical L1 reclaim)**: 기존 operator script `_reclaim/incremental_l1_reclaim.py` 의 4-HEAD verify + sentinel idempotency 패턴을 helper 로 흡수. 신규 발명 0. `_historical_dual_write` batch 종료 hook 추가만 필요. **HIGH feasibility**.
- **ADR amendment**: ADR-027 §D5 (cooperative scheduling) + §D7 (forward path partition boundary) + ADR-029 D1=B (verify-after pattern). 모두 기존 ADR amendment box 패턴 (MCT-163 / MCT-202 / MCT-203 carrier 다수). **HIGH feasibility**.

### §4.3 Continuity (과거 Story/ADR 충돌·중복·의존)

| 의존 Story/ADR | 관계 | 본 Story 영향 |
|---|---|---|
| **MCT-202** (eager post-compaction cleanup, merged 2026-05-18) | forward eager cascade `source_to_delete=Path` 패턴 박제 — today/yesterday 정상 정리 | Layer 1+2 가 forward window invariant 보존, MCT-202 AC 회귀 0 |
| **MCT-189** (`scan_and_cleanup_legacy` WS-B sweep, merged) | 12 cycle 마다 sequential 호출, partition tuple 격리 invariant 신설 카드 | Layer 2 가 sweep 을 별 task 로 분리, INV-A 보강. partition tuple race 차단 |
| **MCT-160 D2** (today + yesterday window, merged) | forward window 정의 SSOT | Layer 1 재사용 (`_discover_partitions_in_range` 호출 시 `start=today-1, end=today`) |
| **MCT-203** (NAS GET sort-phase size-gated cache, merged 2026-05-18) | NAS GET memory invariant 박제 | Layer 1 의 dispatch 수 감소 → MCT-203 cache 효율 동시 개선 (synergy) |
| **MCT-173** (backfill manifest + frozen WAL → L1 historical, merged) | historical L1 130GB 생성 origin | 본 Story 가 그 130GB 의 reclaim 경로 신설 — **complementary** (생성/회수 paired) |
| **MCT-159 #48** (compactor L1 backlog cleanup, OPEN) | orderbookdepth L1 loop `NotImplementedError` 차단 | Layer 1+2+3 모두 `channel != orderbookdepth` 만 적용 (INV-D 의 channel 한정 invariant 정합) |
| **ADR-027 §D5/§D7** (mctrader-hub SSOT) | NAS PUT 4xx fail-fast + forward path boundary | 본 Story 가 두 §에 amendment box 추가 (cooperative scheduling pattern + forward partition discovery boundary) |
| **ADR-029 D1=B** (L1 NAS dual-write enabled, mctrader-hub SSOT) | L1 NAS PUT path 박제 | 본 Story 가 D1=B amendment 에 historical L1 reclaim verify-after pattern 추가 |
| **ADR-034** (NAS object key unification, mctrader-hub SSOT, merged) | `build_nas_key` SSOT helper | Layer 3 의 4-HEAD verify 가 동형 helper 재사용 (recreation 0) |

**충돌 0 확인**: forward window invariant (MCT-202) + sweep cadence (MCT-189) + partition tuple 격리 (Layer 3 INV) 가 partition tuple `(exchange, symbol, channel, date)` 기준 forward (date ∈ [today-1, today]) ↔ historical (date < today-1) 격리로 완전 분리 — race 가능성 0.

## §5 사용자 영향 분석 (RequirementsAnalystAgent — WHY 기반 확장)

### §5.1 디스크 압박 (호소 #1)

| 측정 | 현재 | 본 Story 미해소 시 |
|---|---|---|
| 사용 총량 | 216 GB (one-shot 9GB 회수 후) | 매일 ~30 GB 증가 (5/14~16 일별 30-38 GB 증분 추세) |
| L1 historical 130 GB 잔존 | 회수 0 (영구) | 매일 누적 증가 |
| disk full 시점 | (호스트 capacity 의존) | **MCT-202 trigger 배경** (이전 disk-full 박제) 재발 risk |

→ 사용자 호소 = **디스크 용량 운영 risk** (단순 정리 불편이 아니라 service outage 가능성). MCT-202 와 동일 원인 카테고리 (cleanup 미작동) — 재발 의미.

### §5.2 L3 비축적 (호소 #2)

| 영향 | 현재 |
|---|---|
| L3 dispatch counter | 0 (forward) |
| L3 NAS 객체 | 0 신규 (forward) |
| query 영향 (engine) | engine 은 L1+L2 namespace (candles only, ADR-034 §결정 5 cross-repo isolation) — engine query 직접 영향 0 |
| analytics 영향 (mctrader-data 측 L3 consumer) | L3 = day-level long-window aggregation tier (ADR-009 §D2). day-level query / backtest / archival 모두 L3 의존 — 누락 시 fallback = L2 day-aggregation on-the-fly (비효율) |
| Storage cost 영향 | L2 ↔ L3 compaction ratio 부재 → L2 long-tail 누적 (cold storage 효율 손실) |

→ 사용자 호소 = **observability + analytics + storage cost** 3 측면 영향. engine query 직접 영향 0 이지만 day-level archival/backtest 기능 부재 = 본질적 데이터 파이프라인 결손.

### §5.3 운영자 burden

| 현재 운영자 대응 | burden |
|---|---|
| 매주 one-shot `scan_and_cleanup_legacy` 수동 실행 (`/tmp/one_shot_legacy_cleanup.py --loop --batch 500`) | manual on-call (이번 세션 박제: 31 iter 후에도 L1 0 회수) |
| L1 historical 회수 위해 별 operator script `_reclaim/incremental_l1_reclaim.py` 수동 트리거 | 별 script 유지보수 + run sequence 수동 추적 |
| disk full alarm 매주 page → manual cleanup → unverified cleanup outcome | on-call 빈도 매주, 회수 효율 visibility 0 (no Counter) |

→ **operator burden = 매주 manual on-call + script 2종 수동 chain + cleanup 효율 metric 부재**. 본 Story 가 forward path 격리 + historical reclaim 자동화 + Counter emit 으로 burden 0 화 (사용자 명시 호소 외 derived 요구).

### §5.4 명시-실제 불일치 (Analyst WHY)

| 사용자 명시 표현 | 실제 본질 |
|---|---|
| "이전에 compact 완료된 데이터는 삭제하라" | (요구) cleanup 트리거 추가가 아니라 → (실제) sequential 차단 해소 + forward path scope 축소 |
| "L3로 데이터도 잘 쌓이고 있지 않다" | (요구) L3 dispatch 트리거가 아니라 → (실제) 위와 동일 single origin 해소 |
| (암묵) "다시 호소 안 하게 해라" | (실제) observability metric 신설 (cleanup_cycle_delay / l3_pending / historical_reclaim Counter) → 사용자가 직접 dashboard 로 확인 가능, 호소 재발 차단 |

→ 단순 cleanup 버튼 추가는 본질 해소 0. **Layer 1 (rglob 축소) + Layer 2 (asyncio 분리) + Layer 3 (historical reclaim) 통합 3 layer 가 정답** (Orchestrator brainstorm 결정, 사용자 confirm).

### §5.5 사용자 확인 필요

- **없음**. Orchestrator brainstorm Phase 0 에서 사용자가 "Story 1개에 Layer 1+2+3 통합" 명시 confirm (spec §3 박제). derived 결정 (operator script `_reclaim/incremental_l1_reclaim.py` Phase 2 후속 deprecate, observability metric 3종 신설) 은 사용자 명시 호소 + Researcher Unknown 해소 path 라 PL 단독 결정 가능.

## §6 요구사항 명세 — Acceptance Criteria (RequirementsPLAgent owner)

5+ AC, Given/When/Then 형식. Layer 1+2+3 통합 cross-cut.

### AC-1: forward `_tick` 차단 해소 (Layer 1+2 통합)

- **Given**: forward `_run_l2` worker thread 가 NAS GET 안에서 stall (현재 stall 시간 2h+ 박제)
- **When**: `_tick` next iteration 진입
- **Then**: `scan_and_cleanup_legacy` 호출이 worker stall 과 무관하게 **자기 cadence (≤ 12 × `SCAN_INTERVAL_SECONDS`, default ≤ 5분)** 으로 진행
- **측정**: `mctrader_compactor_cleanup_cycle_delay_seconds` Gauge ≤ 300s (Prometheus)
- **현재 baseline**: ∞ (영원히 도달 안 함)

### AC-2: forward `_run_l2` rglob 범위 축소 (Layer 1)

- **Given**: `<root>/market/<channel>/` 안 L1 파일 16,918 개 (5/13~17 historical 130 GB 포함)
- **When**: forward `_run_l2` 1 invocation 수행
- **Then**: rglob 호출이 iter 하는 file 수 = **forward window (`today-1`, `today`) 안 partition 의 file 수만**, historical (date < today-1) iter 0
- **측정**: integration test `test_compactor_forward_rglob_scope.py` 가 historical partition 생성 후 `_run_l2` 호출 시 historical partition file open count = 0 박제
- **수치 목표**: forward partition file 수 × 1.2 이하 (안전 margin)
- **현재 baseline**: 16,918 file iter (forward + historical 무차별)

### AC-3: historical L1 회수 경로 작동 (Layer 3)

- **Given**: production 5/13~17 historical L1 130 GB partition (4,608 file × 4 day + 2,323 file)
- **When**: operator 가 `promote-historical --root <data_root> --exchange upbit --start 2026-05-13 --end 2026-05-17 --channel orderbooksnapshot` 실행
- **Then**: 각 partition (`exchange, symbol, channel, date`) 의 L2 NAS HEAD 4-tuple verify pass 시 해당 partition 의 L1 local `rglob("part-*.parquet")` unlink + sentinel `.l1-promoted` write
- **측정**: `mctrader_historical_l1_reclaim_total{outcome=ok|skip|fail}` Counter emit. production 5/13~17 partition 의 outcome=ok 합계 ≈ 4,608 + 4,608 + 4,608 + 4,607 + 2,323 = **20,754** (또는 ok+skip 합계, fail = 0)
- **현재 baseline**: 0 (회수 경로 부재)

### AC-4: L3 dispatch 정상 진행

- **Given**: forward window 안 L2 partition 존재 (`<root>/market/<channel>/schema_version=*/tier=L2/.../date=<today|yesterday>`)
- **When**: `_tick` next iteration 진입 (L2 task 완료 무관)
- **Then**: `_run_l3` 가 자기 cadence (`L3_INTERVAL_SECONDS`) 으로 진행하여 forward partition 대상 `_run_l3_for_parquet` dispatch
- **측정**: `dual_write_result_total{tier=L3}` Counter > 0 + `mctrader_l3_pending_partitions{exchange,channel}` Gauge emit
- **현재 baseline**: tier=L3 metric 미존재 (dispatch 0), L3 pending Gauge 부재

### AC-5: forward ↔ historical partition 격리 (race 차단)

- **Given**: forward `_run_l2/_run_l3` 실행 중 + `promote-historical` 동시 실행
- **When**: 두 path 가 같은 `<exchange, symbol, channel>` 의 다른 date partition 처리
- **Then**: forward path = date ∈ [today-1, today] **only**, historical path = date < today-1 **only** — partition tuple 0-overlap
- **측정**: integration test `test_compactor_tick_isolation.py` 가 forward + historical 동시 시뮬레이션 후 두 path 가 access 한 partition tuple set 의 intersection = ∅ 박제
- **수치 목표**: 0 overlap (strict)
- **현재 baseline**: 검증 부재 (race 가능성 잠재)

### AC-6: per-step stall timeout (starvation 차단)

- **Given**: 임의 step (L2/L3/cleanup/historical-reclaim) 의 단일 invocation 이 비정상 stall
- **When**: 경과 시간 > `MCTRADER_COMPACTOR_STEP_TIMEOUT_SECONDS` (default 600s)
- **Then**: 해당 step 의 task 가 cancel + log warning emit + 다음 step 진행
- **측정**: `mctrader_compactor_step_stall_seconds{step}` Gauge + log "[compactor] step timeout" pattern emit
- **현재 baseline**: stall 영구 (timeout/circuit breaker 부재, Researcher Unknown #2)

### AC-7: observability — 사용자 호소 재발 차단

- **Given**: 본 Story merge + production 배포 24h 경과
- **When**: 사용자가 Prometheus dashboard 조회
- **Then**: 다음 4 metric 모두 emit 정상:
  1. `mctrader_compactor_cleanup_cycle_delay_seconds` ≤ 300s
  2. `mctrader_historical_l1_reclaim_total{outcome=ok}` > 0 (production 회수 진행 중)
  3. `mctrader_l3_pending_partitions` Gauge emit + 정상 감소
  4. `dual_write_result_total{tier=L3}` Counter > 0
- **현재 baseline**: 4 metric 모두 미존재 또는 0 — 사용자가 호소 재발 시까지 visibility 0

---

### AC-INV 매핑 (Architect lane §5 INV 작성 prereq)

본 lane 이 AC 까지만 책임. INV (Invariants) 는 Architect lane 이 §5 INV / §6 위험 section 신설 시 작성. AC ↔ INV 예상 매핑 (Architect 참고용):

| AC | 예상 INV (Architect 작성) |
|---|---|
| AC-2 | INV-A: forward path historical 미접근 (`_discover_partitions_in_range` 강제 사용 grep gate) |
| AC-3 | INV-D: sentinel `.l1-promoted` 멱등 (재실행 safe) |
| AC-5 | INV-B: promote-historical today/yesterday 미접근 + INV-C: L2 NAS HEAD verify fail 시 L1 unlink 0 |
| AC-6 | INV-E: per-step timeout 후 다음 step 진입 (starvation 차단) |

## §7 의존성 / Architect §5 INV / §6 위험

(ArchitectPLAgent lane write — TBD. §6 AC 의 AC-INV 매핑 + §4.3 Continuity 표 참고)

## §8 Test Contract

(TestContractArchitectAgent deputy write — TBD)

## §9 Phase 구조

(ArchitectPLAgent lane write — TBD)

## §10 FIX Ledger

(Orchestrator monopoly — CFP-32. append-only)

| # | Lane | Root Cause | Fix Summary | Outcome |
|---|------|------------|-------------|---------|

## §10.5 Git Ops Log

(GitOpsAgent 단독 owner — append-only)

| Event | 시각 | Actor | Detail | Outcome |
|-------|------|-------|--------|---------|
| WORKTREE_CREATE | 2026-05-19T00:00:00Z | GitOpsAgent | .claude/worktrees/mct-204 on branch mct-204-compactor-tick-stall-historical-l1-reclaim (base=origin/main) | SUCCESS |

## §11 RETRO

(PMOAgent lane write — Phase 2 merge 후 TBD)
