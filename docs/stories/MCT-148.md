---
story_key: MCT-148
story_scope: data
story_issues:
  - repo: mclayer/mctrader-hub
    number: 248
status: phase:요구사항
epic_milestone: Epic-cold-tier-nas-minio
parent_epic: EPIC-cold-tier-nas-minio
created_at: 2026-05-12
delegates: []
---

# MCT-148: Cold tier PoC spike — 5종 검증 (Stage 1 Feasibility Spike)

- **Issue**: [mclayer/mctrader-hub#248](https://github.com/mclayer/mctrader-hub/issues/248) (cross-repo, hub 측 Story 추적)
- **Epic**: EPIC-cold-tier-nas-minio (Stage 1 Feasibility Spike)
- **Status**: phase:요구사항
- **Delegates**: 없음 (data-only — spike)
- **Phase**: phase1_only (spike — src 무변경, 결과 박제 후 종료)
- **Milestone**: mctrader-hub#3 Epic-cold-tier-stage-1-spike
- **이전 Story**: MCT-147 MERGED (mctrader-hub PR #246 409d076 — NAS endpoint 가용)
- **다음 Story**: MCT-149 (ADR-027 본문 author — 본 Story 의 evidence pack 인용)

## §1 사용자 요구사항 (verbatim)

### WHY (Stage 1 Feasibility Spike — 사용자 명시 동기)

Stage 1 Feasibility Spike. Stage 2 진입 (실 cutover MCT-150~155) 전에 NAS MinIO endpoint 의 운영 가능성을 5종 PoC test 로 **정량 측정 + 박제**. 측정 결과 = MCT-149 ADR-027 본문 의 D8 (Spike scope) evidence 인용. 본 Story 무 결정 (decision 0) — 측정 + 박제만.

### Stage 1 PoC scope — 5종 검증 항목 (D8 amend 반영)

1. **HTTP 200 health check** (D2 amend → TLS handshake 제외, HTTP 200 만 검증)
2. **Latency baseline** — 1KB / 1MB / 10MB / 50MB PUT 각 N회. p50/p95/p99 측정
3. **Large file PUT 50MB** — single PUT 50MB parquet 가용. multipart 자동 분할 여부 확인
4. **Restart idempotency** — MinIO 컨테이너 5min down → up. boto3 retry 후 동일 endpoint 정상 PUT
5. **Partial object visibility** — 50MB PUT 진행 중 GET 시도 → "object not found" 또는 "incomplete read" 응답 (절대로 partial bytes 보이면 안 됨, S3 atomic invariant)

### MCT-147 사전 검증 결과 (본 Story 의 입력 자료)

사용자가 NAS Container Manager 에 MCT-147 의 docker-compose 적용 → endpoint 가용 박제 완료:

- **NAS_HOST**: `192.168.50.200` (LAN 내부)
- **MinIO endpoint (S3 API)**: `http://192.168.50.200:9000`
- **Console (UI)**: `http://192.168.50.200:9001`
- **Credential**: `MINIO_ROOT_USER=mctrader-admin` / `MINIO_ROOT_PASSWORD` (PoC test 는 환경변수 `NAS_MINIO_*` 으로 주입, mctrader-data 측 test 는 별도 환경변수 mechanism — mctrader-hub `.env` 는 gitignored, 본 repo 측 재사용 X)
- **Bucket**: `mctrader-market` (이미 존재, MCT-147 AC-1 PASS)
- **NAS 디스크**: 10 TiB total / 5.3% used (충분한 capacity headroom)
- **사전 PUT/GET roundtrip**: 1KB sha256 IDENTICAL (correctness 사전 확인)
- **Health response**: 23ms (LAN gigabit baseline)

### 산출물 (planned_files)

- `tests/spike/test_nas_minio_http_health.py` (D2 amend 정합 — 원안 `test_nas_minio_tls_handshake.py` rename)
- `tests/spike/test_nas_minio_latency_baseline.py`
- `tests/spike/test_nas_minio_large_put_50mb.py`
- `tests/spike/test_nas_minio_restart_idempotency.py`
- `tests/spike/test_nas_minio_partial_visibility.py`
- `tests/spike/conftest.py` (NAS endpoint 환경변수 fixture)
- `docs/stories/MCT-148.md` — 본 Story file
- `.tmp/evidence-pack-MCT-148.md` — 5종 PoC 결과 박제 (gitignored)

### 11 결정점 중 본 Story 적용 (scope_manifest 인용)

- **D2 TLS/auth**: amend 반영 — TLS handshake 검증 제외, HTTP 200 health 만 검증 (Stage 1 LAN 내부망 한정)
- **D6 invariant**: 본 Story 가 측정 actor — partial visibility test 가 S3 atomic invariant 정량 검증
- **D8 spike scope**: 5종 PoC 검증 항목 — 본 Story 가 모두 cover
- **D10 영향 repo**: data (본 Story, tests/spike/ 한정 — src/ 무변경)

### 의존

- **upstream**: MCT-147 MERGED (NAS endpoint 가용)
- **downstream**: MCT-149 (ADR-027 본문 author — 본 Story evidence pack 인용)

### 컨텍스트 (사전 박제, 재발견 비용 0)

- Brainstorm spec: `c:/workspace/mclayer/mctrader-hub/.tmp/brainstorm-cold-tier-nas-minio.md`
- Plan: `c:/workspace/mclayer/mctrader-hub/.tmp/plan-cold-tier-nas-minio.md`
- scope_manifest: `c:/workspace/mclayer/mctrader-hub/scope_manifests/EPIC-cold-tier-nas-minio.yaml` (D8 amend: TLS handshake → HTTP 200 health check)
- ADR-027 reservation marker: `c:/workspace/mclayer/mctrader-hub/docs/adr/.reservation-ADR-027.md` (D1~D11 박제)
- MCT-147 §11 retro: `c:/workspace/mclayer/mctrader-hub/docs/stories/MCT-147.md` (§11 7 subsection — MCT-148 진입 의무 박제)

## §2 도메인 해석 (DomainAgent 통합)

### 내부 제약 (mctrader 6-repo 도메인 해석)

**관찰 1 — `MCTRADER_DATA_ROOT` SSOT 의 cold tier extension (ADR-009 §D12.1)**: mctrader-data 의 storage path SSOT 는 `MCTRADER_DATA_ROOT` 환경변수 + Docker named volume `mctrader_data`. NAS MinIO 는 이 SSOT 의 **cold tier extension** (L2/L3) — 본 spike 는 NAS endpoint 만 측정, hot path SSOT 변경 0. test fixture 의 `NAS_MINIO_ENDPOINT` 환경변수는 hot path env 와 namespace 분리 (`MINIO_ENDPOINT` 와 별개 — production cutover 인 MCT-155 까지 절대 swap 금지).

**관찰 2 — ADR-017 forward-only invariant 정합 (compactor write-then-rename ↔ NAS upload semantic mapping)**: ADR-017 §3 의 atomic write-then-rename invariant 는 local file system 의 rename(2) atomic primitive 에 의존. NAS MinIO 의 S3 PUT 은 application-layer atomic semantic 제공 — 본 spike 의 **partial visibility test** 가 이 mapping 의 정합 확인 actor (PUT 진행 중 GET = not found / incomplete-error, partial bytes 0). compactor 의 atomic write-then-rename pattern 이 NAS upload 로 mapping 시 invariant 보존 입증.

**관찰 3 — `minio_uploader.py` boto3 client 의 baseline test fixture**: 기존 `src/mctrader_data/compactor/minio_uploader.py` 는 boto3 S3 client + `BUCKET_NAME='mctrader-market'` 하드코딩 패턴. 본 spike 의 5 test 가 동일 boto3 패턴 사용 → MCT-150 (compactor hardening) 의 **baseline test fixture** 로 직접 reuse. test 코드의 boto3 config (retry policy / multipart threshold) 가 MCT-150 production hardening 의 starting point.

**관찰 4 — ADR-027 D8 reservation 정합**: ADR-027 reservation marker §D8 (Spike scope) 의 5 PoC 항목 박제 → 본 Story 는 5 항목 모두 cover + 측정값 박제 → MCT-149 (ADR-027 본문 author) 의 evidence 직접 인용. 본 Story 무 결정 (decision 0) — ADR-027 본문의 D1~D7, D9~D11 결정은 MCT-149 scope.

### 도메인 용어 정합

- **PoC (Proof of Concept) spike**: Stage 1 feasibility 측정 단계. src/ 변경 0, test/ 만 신규. 측정값 박제 후 종료 (production code 진입 X — MCT-150~155 scope).
- **evidence pack**: `.tmp/evidence-pack-MCT-148.md` — 5 test 결과 + raw log + 측정값 통합 박제. gitignored (test artifact, repo 누적 X). MCT-149 본문 author 시 인용 (ADR-027 본문에 측정값 그대로 transcribe).
- **S3 atomic invariant**: PUT 진행 중 object 는 client 에 절대 visible 하지 않음. AWS S3 spec + MinIO 구현 모두 보장. 본 spike 의 partial visibility test 가 NAS 환경에서 invariant 정량 확인.
- **boto3 retry policy**: default 3회 exponential backoff. 본 spike 의 restart idempotency test 가 default retry 충분성 확인 — MCT-150 hardening 에서 retry 횟수/backoff 조정 필요 여부 판단 input.

## §3 관련 ADR / 코드 경로

### 관련 ADR (강한 관련 — 직접 제약)

- **ADR-009** (Data SSOT) §D12.1: `MCTRADER_DATA_ROOT` 환경변수 SSOT — 본 Story 의 test fixture 가 `NAS_MINIO_*` namespace 분리로 hot path env (`MINIO_ENDPOINT`) 침범 0 보장.
- **ADR-017** (Zero-loss invariant) §3: atomic write-then-rename invariant — 본 Story 의 partial visibility test 가 S3 PUT atomic semantic 정량 확인 (NAS environment 정합 입증).
- **ADR-016** (Hot path zero-loss) §A4: 네트워크 파일시스템 위 named volume 금지 — 본 Story 는 application-layer S3 API (boto3 over HTTP) 만 사용, POSIX semantic 의존 0. 정합.
- **ADR-027** (reserved) §D8: Spike scope 5 PoC — 본 Story 가 직접 cover + measurement 박제 → MCT-149 본문 author 의 evidence 입력.

### 관련 ADR (약한 관련 — 배경)

- **ADR-008** (Secret management): credential 평문 노출 0 — 본 Story 의 test fixture 가 `NAS_MINIO_*` 환경변수 주입 (코드 hardcoding 0), evidence pack 의 log 박제 시 credential mask 의무.
- **ADR-026** (Legacy Candle Provenance Policy): cold tier 의 immutable invariant — 본 Story 비대상 (versioning 정책 결정은 MCT-149 ADR-027 본문 author scope).

### 관련 코드 경로

- `src/mctrader_data/compactor/minio_uploader.py` — 기존 compactor boto3 client (BUCKET_NAME='mctrader-market' 하드코딩). **본 Story 변경 0** — baseline pattern reference 만.
- `src/mctrader_data/config/` — `MINIO_ENDPOINT` env 정의. **본 Story 변경 0** — test fixture 가 `NAS_MINIO_ENDPOINT` 별도 namespace 사용.
- `tests/spike/` — 신규 디렉터리 (본 Story 가 생성). pytest collection 자동 인식.
- `tests/conftest.py` (기존) — repo-wide fixture. **본 Story 변경 0** — `tests/spike/conftest.py` 가 spike 한정 fixture 만 author.
- `pyproject.toml` — pytest config. **본 Story 변경 0** — `tests/spike/` 가 default test discovery path 에 자동 포함 (`tests/` glob 정합).

## §4 Change Impact Analysis

### §4.1 Change Impact (mctrader-data `tests/spike/` 신규 디렉터리 + Story file + evidence pack)

**AS-IS**:
- `tests/spike/` 디렉터리 부재 — 기존 `tests/` 하위는 unit/integration 한정 (`integration/`, `cold/`, `compactor/`, `wal/` 등)
- `docs/stories/` 디렉터리 빈 상태 (`.gitkeep` 만 존재) — 본 Story 가 mctrader-data 측 **첫 Story file**
- NAS endpoint 가용 (MCT-147 사전 검증 완료)

**DELTA (신규 산출물)**:
- `tests/spike/test_nas_minio_http_health.py` — 신규
- `tests/spike/test_nas_minio_latency_baseline.py` — 신규
- `tests/spike/test_nas_minio_large_put_50mb.py` — 신규
- `tests/spike/test_nas_minio_restart_idempotency.py` — 신규
- `tests/spike/test_nas_minio_partial_visibility.py` — 신규
- `tests/spike/conftest.py` — 신규 (`NAS_MINIO_*` env fixture + SKIP-on-missing)
- `docs/stories/MCT-148.md` — 본 file
- `.tmp/evidence-pack-MCT-148.md` — 신규 gitignored (5 test 측정값 + log)

**Cross-repo 영향**: 없음. 본 Story scope = mctrader-data tests/ + docs/ 한정. hub/engine/web/market/market-bithumb 5 repo 모두 변경 0. (MCT-147 의 NAS endpoint 는 hub repo 가 deploy 완료, 본 Story 는 endpoint 의 consumer test 만.)

**기존 파일 수정**: 없음 — `src/` 무변경 의무 (Phase pair = phase1_only spike). `pyproject.toml` 의 pytest config 변경 0 (default discovery 자동 인식).

### §4.2 Feasibility (FeasibilityAgent 평가)

**Prerequisite 1 — pytest + boto3 가용**: mctrader-data 의 `pyproject.toml` 의존성. pytest 는 기존 test suite 가 사용 중, boto3 는 `minio_uploader.py` 가 사용 중 — 신규 의존성 0.

**Prerequisite 2 — NAS endpoint 가용 + credential 환경변수 주입**: MCT-147 사전 검증 완료 (`http://192.168.50.200:9000` healthy). 사용자가 본 Story 의 PoC test 실행 시 환경변수 (`NAS_MINIO_ENDPOINT`, `NAS_MINIO_ACCESS_KEY`, `NAS_MINIO_SECRET_KEY`) 주입 의무. CI 환경에서는 환경변수 부재 → SKIP (CI 친화 — AC-1 의 SKIP behavior).

**Prerequisite 3 — Docker Desktop (사용자 Windows 환경) — restart idempotency test 한정**: restart idempotency test 는 NAS 측 컨테이너의 down/up 을 trigger 해야 함. 두 옵션:
- **옵션 A**: 사용자가 NAS Container Manager UI 에서 manual stop/start (5min 간격). test 는 manual gate 형태 (`pytest -m manual`).
- **옵션 B**: SSH 접근 가능 시 `docker compose restart` 원격 trigger. test fixture 의 옵션 선택 ArchitectPLAgent 단계 결정.

**Prerequisite 4 — large file 50MB sample data**: 본 spike 는 random bytes (`os.urandom(50 * 1024 * 1024)`) 사용 가능 — production parquet 와 동일 size 만 확보. 측정 metric (latency / success rate) 은 payload entropy 무관.

**구현 가능성**: HIGH — 5 test 모두 boto3 standard API 만 사용, 신규 dependency 0, NAS endpoint 가용 박제 완료. partial visibility test 는 threading + race-window 활용으로 single test process 내 구현 가능 (별도 fixture 불필요).

**ADR 충돌 후보**: 없음 — ADR-016/017 의 hot path 제약과 정합 (cold tier 만 spike), ADR-009 SSOT 정합 (test fixture 가 namespace 분리), ADR-027 reservation 의 D8 직접 cover.

### §4.3 Continuity (이전 작업과의 충돌/중복/의존)

**MCT-147 D2 amend 정합**: MCT-147 의 D2 amend (TLS handshake → HTTP 200 health) 가 본 Story 의 첫 번째 test (`test_nas_minio_http_health.py`) 에 직접 반영 — 원안 `test_nas_minio_tls_handshake.py` 명칭 rename 완료.

**D8 spike scope 5 항목 모두 cover**: MCT-147 §11 retro 의 "MCT-148 진입 의무" 박제 확인 — 5 항목 매핑:
- D8 (1) HTTP health → `test_nas_minio_http_health.py`
- D8 (2) Latency baseline → `test_nas_minio_latency_baseline.py`
- D8 (3) Large file PUT → `test_nas_minio_large_put_50mb.py`
- D8 (4) Restart idempotency → `test_nas_minio_restart_idempotency.py`
- D8 (5) Partial visibility → `test_nas_minio_partial_visibility.py`

**MCT-149 ADR-027 본문 author 의 evidence 입력 의무**: 본 Story 의 `.tmp/evidence-pack-MCT-148.md` 가 MCT-149 ADR-027 D8 (Spike scope) section 의 evidence 직접 인용. evidence pack 의 schema (5 sub-section, 각 sub-section = test 별 측정값 + raw log + PASS/FAIL) 는 ArchitectPLAgent 단계에서 §8 sub-section 으로 정식화.

**`minio_uploader.py` 무변경 정합 (MCT-150 scope)**: 본 Story 는 endpoint cutover X — `minio_uploader.py` 의 `MINIO_ENDPOINT` env 는 local minio container 유지. 본 spike test 는 `NAS_MINIO_*` 별도 namespace 만 사용 → production code path 의 동작 영향 0.

**ADR-017 zero-loss invariant 무영향**: hot path (collector / WAL / L1) 변경 0. 본 Story 는 cold tier endpoint 측정만 — invariant 무영향.

**중복 작업 없음**: 기존 `tests/spike/` 디렉터리 부재 — 신규 생성. 기존 test 의 NAS endpoint 접근 0 — 신규 fixture (`tests/spike/conftest.py`) 만 author.

**ADR-027 reservation amend 누락 잔존 (MCT-149 scope)**: MCT-147 의 D2 amend (TLS → HTTP) 가 scope_manifest 에는 반영되었으나 **ADR-027 reservation marker (`.reservation-ADR-027.md`) 의 D2 wording 갱신은 미수행 잔존**. MCT-149 의 ADR-027 본문 publish 단계에서 일괄 반영 의무 (본 Story §11 retro 시 박제). 본 Story 의 test 명칭 (`test_nas_minio_http_health.py`) 은 amend 정합 — wording mismatch 영향 0.

## §5 Acceptance Criteria

### AC (Given/When/Then 형식)

- **AC-1 (5 PoC test pytest PASS)**: GIVEN NAS endpoint 환경변수 (`NAS_MINIO_ENDPOINT`, `NAS_MINIO_ACCESS_KEY`, `NAS_MINIO_SECRET_KEY`) 주입 + NAS endpoint 가용, WHEN `pytest tests/spike/` 실행, THEN 5 test 모두 PASS. **환경변수 부재 시 → 모두 SKIP** (CI 친화, EC-1 정합).
- **AC-2 (evidence pack 5 sections 박제)**: GIVEN 5 PoC test 실행 완료, WHEN `.tmp/evidence-pack-MCT-148.md` 작성, THEN 5 sub-section 박제 (test 별 측정값 + raw log + PASS/FAIL) — MCT-149 ADR-027 본문 evidence 인용 가능 schema.
- **AC-3 (Latency NFR 충족)**: GIVEN latency baseline test 실행 + N회 반복, WHEN p50/p95/p99 측정 + evidence 박제, THEN p99 < 500ms (1KB PUT 기준) AND p99 < 5s (50MB PUT 기준) — LAN gigabit 기대값 (MCT-147 사전 health 23ms baseline 기반).
- **AC-4 (Restart idempotency 5min recovery)**: GIVEN MinIO 컨테이너 healthy 상태에서 PUT N회 박제, WHEN NAS 측 컨테이너 5min down → up, THEN boto3 default retry (3회 exponential backoff) 후 동일 endpoint PUT 성공 + bucket 의 사전 박제 object 보존 (NAS local volume durability).
- **AC-5 (Partial visibility S3 atomic invariant)**: GIVEN 50MB PUT 진행 중 (threading race-window), WHEN 동일 object key 로 GET 시도, THEN response 가 **"object not found" 또는 "incomplete read" 에러** — **절대로 partial bytes 클라이언트 visible X** (S3 atomic invariant strictly 준수 확인).

### NFR 측정 항목 (evidence pack 박제 의무)

- **NFR-1 (latency p99 1KB)**: < 500ms (LAN gigabit baseline, MCT-147 health 23ms 기반 충분한 headroom)
- **NFR-2 (large PUT 50MB success rate)**: 100% (3회 반복)
- **NFR-3 (restart recovery time)**: ≤ 5min (boto3 default retry 충분)
- **NFR-4 (partial visibility)**: 0 row partial (S3 atomic invariant strictly)
- **NFR-5 (5 test 총 wall-clock)**: < 15min (1 cycle CI 기준 — manual gate test 제외)

### Edge cases

- **EC-1 (NAS endpoint 환경변수 미설정 시 SKIP)**: CI 환경 또는 로컬 환경에서 `NAS_MINIO_ENDPOINT` 미설정 시 — `tests/spike/conftest.py` 가 fixture 단계에서 `pytest.skip()` 호출. 5 test 모두 SKIP, exit code 0 (CI 친화).
- **EC-2 (1차 PUT 실패 시 boto3 default retry)**: NAS endpoint 일시적 네트워크 hiccup 시 — boto3 default 3회 exponential backoff retry. final 결과 (PASS/FAIL) + retry 횟수 + 각 시도의 latency 모두 evidence pack 박제.
- **EC-3 (Restart idempotency test 의 manual gate 의존)**: NAS 측 컨테이너 down/up trigger 방법 (manual UI vs SSH `docker compose restart`) — ArchitectPLAgent 단계 결정. manual gate 의 경우 `pytest -m manual` marker 사용, evidence pack 박제 시 manual gate 진행 timestamp 명시.
- **EC-4 (50MB single PUT vs multipart 자동 분할)**: boto3 의 default multipart threshold 는 5MB 부근 — 50MB PUT 시 multipart 자동 활성. 본 spike 는 multipart 분할 발생 여부 + 분할 시 PUT 성공률 모두 evidence 박제 (NFR-2 success rate 100% 충족 여부 검증).
- **EC-5 (Partial visibility test 의 race window 확보)**: 50MB PUT 의 wall-clock 시간 (LAN gigabit 기준 ~5s) 내에 GET 시도 — threading 으로 PUT future + GET future 병렬 실행. race-window 확보 실패 시 (PUT 가 너무 빠른 경우) FAIL 아닌 EVIDENCE INSUFFICIENT 처리 — payload size 증가 또는 throttling 시도 박제.

### Non-goals (본 Story 비대상)

- **`src/` 변경**: MCT-150 scope (compactor minio_uploader.py hardening — retry policy 조정 / endpoint config 추가).
- **Production endpoint cutover**: MCT-155 scope (compactor 의 `MINIO_ENDPOINT` env 가 NAS endpoint 로 swap).
- **TLS 검증**: D2 amend 로 제외 (Stage 1 한정 HTTP, MCT-149 D2 wording 갱신 시 Stage 2 TLS 재검토).
- **ADR-027 본문 author**: MCT-149 scope — 본 Story 의 evidence pack 이 D8 section evidence 입력.
- **Multi-cycle stress test**: 본 spike 는 1 cycle 측정만 (5 test 총 wall-clock < 15min 의 NFR-5 정합). long-running stability test 는 Stage 2 scope.
- **Monitoring / Alerting setup**: Stage 2 scope (Prometheus exporter + alert rule).

### 사용자 확인 필요 (blocking — ArchitectPLAgent 진입 전 해소 의무)

- **Q1 (Issue 번호 박제)**: 본 Story 의 GitHub Issue 번호 — Orchestrator 의 story-init.yml Action 이 생성 후 frontmatter `story_issues[0].number` + § header `Issue: TBD` 자리 swap. 본 Story file 의 frontmatter `number: TBD` + § header `Issue: TBD` 둘 다 후속 작업.
- **Q2 (Restart idempotency test trigger 방법 선택)**: NAS 측 MinIO 컨테이너의 down/up trigger 방법 — **옵션 A** (사용자 manual UI gate, `pytest -m manual`) vs **옵션 B** (SSH `docker compose restart` 원격 trigger). 옵션 B 선택 시 SSH credential 환경변수 fixture 추가 필요. ArchitectPLAgent 단계 §6 Design 에서 결정.
- **Q3 (Latency baseline 반복 횟수 N)**: latency baseline test 의 PUT 반복 횟수 N — p50/p95/p99 통계적 유의성 확보 vs wall-clock 시간 (NFR-5 < 15min) trade-off. 후보값: N=30 (각 size 별, 총 4 size × 30 = 120 PUT, ~5min 예상) vs N=100 (~15min 예상, NFR-5 임계). ArchitectPLAgent 단계 §6 Design 에서 결정.
- **Q4 (evidence pack 형식 schema)**: `.tmp/evidence-pack-MCT-148.md` 의 5 sub-section schema — 각 sub-section 의 fields (header / measured values table / raw log excerpt / PASS/FAIL verdict / observed anomalies). MCT-149 ADR-027 D8 section 의 transcription 편의 고려. ArchitectPLAgent 단계 §8 sub-section 으로 정식화.
- **Q5 (partial visibility test 의 race-window 확보 전략)**: 50MB PUT 의 wall-clock 시간이 LAN gigabit 기준 너무 짧을 경우 (e.g., ~3s) race-window 확보 실패 가능. 후보 전략: payload size 100MB 증가 / boto3 client-side throttling / multiple GET sampling (50ms 간격). ArchitectPLAgent 단계 §6 Design 에서 결정.

---

## §6 Design (Change Plan — ArchitectAgent chief author, TestContractArch chief contributor)

### §6.0 설계 결정 trail (Q2~Q5 사용자 답변 + ArchitectPL 결정 반영)

**Q2 — Restart idempotency trigger 결정 = 옵션 A (사용자 manual DSM Container Manager UI STOP/START)**
- 근거: SSH 접근 불요, NAS 측 추가 fixture 0. test 는 5min sleep 동안 사용자 manual gate, boto3 retry-and-recover 변수만 측정.
- 영향: §6.5 test 가 `print()` 로 manual gate 안내 + `time.sleep(300)` 으로 timeout fallback 박제 + `pytest.mark.manual` marker.

**Q3 — Latency N = 30 (각 file size 별)**
- 근거: 4 size (1KB/1MB/10MB/50MB) × 30 PUT = 120 PUT total. wall-clock 5-10min 예상 — NFR-5 (< 15min) 임계 안. statistical p99 확보 (N=30 이면 standard error 충분).
- 영향: §6.3 test 의 `N_REPS=30` 상수 + `pytest.mark.parametrize` 4 size matrix.

**Q4 — Evidence pack schema (ArchitectPL 결정)**: markdown table 표준
- 각 test 별 row: (PASS/FAIL, 측정값 dict, raw log excerpt, sha256, ISO-8601 timestamp). JSON code block 형태로 `.tmp/evidence-pack-MCT-148.md` 에 append.
- §6.7 의 `evidence_log` fixture 가 schema 강제 — 모든 test 가 동일 형식 append.

**Q5 — Partial visibility race-window (ArchitectPL 결정)**: multi-thread approach
- main thread 가 50MB PUT 시작, 0.1s 후 별도 thread 가 50ms 간격으로 GET 시도. PUT 완료 (`put_done` Event) 까지 반복. S3 atomic invariant: GOT → 반드시 SIZE_50MB, ERROR → NoSuchKey 만 허용.
- 근거: single-process threading 으로 race window 확보 — payload size 증가 또는 throttling 불요 (Q5 의 후보 중 가장 단순).

### §6.1 `tests/spike/conftest.py` — NAS endpoint fixture + evidence_log fixture

```python
# tests/spike/conftest.py
import os
import datetime
import json
from pathlib import Path

import pytest

NAS_MINIO_ENDPOINT = os.environ.get("NAS_MINIO_ENDPOINT")
NAS_MINIO_ACCESS_KEY = os.environ.get("NAS_MINIO_ACCESS_KEY")
NAS_MINIO_SECRET_KEY = os.environ.get("NAS_MINIO_SECRET_KEY")
NAS_MINIO_BUCKET = os.environ.get("NAS_MINIO_BUCKET", "mctrader-market")

skip_if_no_nas = pytest.mark.skipif(
    not all([NAS_MINIO_ENDPOINT, NAS_MINIO_ACCESS_KEY, NAS_MINIO_SECRET_KEY]),
    reason="NAS_MINIO_* env vars not set (PoC spike — SKIP on CI without NAS endpoint)",
)


@pytest.fixture(scope="session")
def s3_client():
    import boto3
    from botocore.config import Config

    return boto3.client(
        "s3",
        endpoint_url=NAS_MINIO_ENDPOINT,
        aws_access_key_id=NAS_MINIO_ACCESS_KEY,
        aws_secret_access_key=NAS_MINIO_SECRET_KEY,
        config=Config(retries={"max_attempts": 3, "mode": "standard"}),
    )


@pytest.fixture(scope="session")
def bucket():
    return NAS_MINIO_BUCKET


@pytest.fixture(scope="session")
def evidence_log():
    """Append evidence rows to .tmp/evidence-pack-MCT-148.md (gitignored).

    Schema (Q4 결정): JSON code block per append, ISO-8601 timestamp header.
    """
    log_path = Path(".tmp/evidence-pack-MCT-148.md")
    log_path.parent.mkdir(exist_ok=True)
    if not log_path.exists():
        log_path.write_text("# Evidence pack — MCT-148 PoC 5종\n\n", encoding="utf-8")

    def append(row: dict) -> None:
        ts = datetime.datetime.now().isoformat()
        line = f"\n## {ts}\n\n```json\n{json.dumps(row, indent=2, default=str)}\n```\n"
        with log_path.open("a", encoding="utf-8") as fp:
            fp.write(line)

    return append


def pytest_configure(config):
    config.addinivalue_line(
        "markers", "manual: requires manual user gate (e.g., DSM UI STOP/START)"
    )
```

**SecurityArch 검증**: `NAS_MINIO_*` 환경변수 = production credential (`MINIO_ENDPOINT` / `MINIO_ROOT_*`) 와 **namespace 격리** — production code path 침범 0. hardcoded credential 0 (test 코드 grep `MINIO_ROOT_USER` / `mctrader-admin` 0 hit 의무, §8.3 ST1).

### §6.2 `tests/spike/test_nas_minio_http_health.py` (D2 amend — TLS handshake 제외, HTTP 200 만)

```python
# tests/spike/test_nas_minio_http_health.py
"""PoC 1 — HTTP 200 health check (D2 amend, Stage 1 LAN HTTP 운영)."""
import urllib.request

from tests.spike.conftest import NAS_MINIO_ENDPOINT, skip_if_no_nas


@skip_if_no_nas
def test_http_health_live_returns_200():
    health_url = NAS_MINIO_ENDPOINT.rstrip("/") + "/minio/health/live"
    req = urllib.request.Request(health_url)
    with urllib.request.urlopen(req, timeout=5) as resp:
        assert resp.status == 200, f"expected 200, got {resp.status}"


@skip_if_no_nas
def test_http_health_ready_returns_200():
    ready_url = NAS_MINIO_ENDPOINT.rstrip("/") + "/minio/health/ready"
    req = urllib.request.Request(ready_url)
    with urllib.request.urlopen(req, timeout=5) as resp:
        assert resp.status == 200, f"expected 200, got {resp.status}"
```

### §6.3 `tests/spike/test_nas_minio_latency_baseline.py` — 4 size × 30 = 120 PUT

```python
# tests/spike/test_nas_minio_latency_baseline.py
"""PoC 2 — Latency baseline (Q3: N=30 per size, 4 size × 30 = 120 PUT total)."""
import os
import statistics
import time

import pytest

from tests.spike.conftest import skip_if_no_nas

N_REPS = 30
SIZES = [
    ("1KB", 1024),
    ("1MB", 1024 * 1024),
    ("10MB", 10 * 1024 * 1024),
    ("50MB", 50 * 1024 * 1024),
]
P99_LIMIT_MS = {"1KB": 500, "1MB": 1000, "10MB": 3000, "50MB": 10000}


@skip_if_no_nas
@pytest.mark.parametrize("size_label,size_bytes", SIZES)
def test_latency_baseline_per_size(s3_client, bucket, size_label, size_bytes, evidence_log):
    payload = os.urandom(size_bytes)
    durations_ms = []
    for i in range(N_REPS):
        key = f"spike/latency/{size_label}/iter-{i}.bin"
        t0 = time.perf_counter()
        s3_client.put_object(Bucket=bucket, Key=key, Body=payload)
        durations_ms.append((time.perf_counter() - t0) * 1000)

    p50 = statistics.median(durations_ms)
    p95 = statistics.quantiles(durations_ms, n=20)[18]
    p99 = statistics.quantiles(durations_ms, n=100)[98]

    evidence_log({
        "test": "latency_baseline",
        "size": size_label,
        "n_reps": N_REPS,
        "p50_ms": round(p50, 2),
        "p95_ms": round(p95, 2),
        "p99_ms": round(p99, 2),
        "min_ms": round(min(durations_ms), 2),
        "max_ms": round(max(durations_ms), 2),
        "samples": [round(d, 2) for d in durations_ms],
    })

    # cleanup
    for i in range(N_REPS):
        s3_client.delete_object(Bucket=bucket, Key=f"spike/latency/{size_label}/iter-{i}.bin")

    assert p99 < P99_LIMIT_MS[size_label], (
        f"p99 {p99:.1f}ms > limit {P99_LIMIT_MS[size_label]}ms for {size_label}"
    )
```

**NFR mapping**: 1KB p99 < 500ms (NFR-1) / 50MB p99 < 10s (NFR 보강) — AC-3 충족.

### §6.4 `tests/spike/test_nas_minio_large_put_50mb.py` — 3 회 반복, sha256 roundtrip

```python
# tests/spike/test_nas_minio_large_put_50mb.py
"""PoC 3 — Large PUT 50MB (3 회 반복, multipart 자동 분할 + sha256 roundtrip)."""
import hashlib
import os

import pytest

from tests.spike.conftest import skip_if_no_nas

SIZE_50MB = 50 * 1024 * 1024
N_REPS = 3


@skip_if_no_nas
@pytest.mark.parametrize("iteration", range(N_REPS))
def test_large_put_50mb(s3_client, bucket, iteration, evidence_log):
    payload = os.urandom(SIZE_50MB)
    sha_local = hashlib.sha256(payload).hexdigest()
    key = f"spike/large/50mb-iter-{iteration}.bin"

    s3_client.put_object(Bucket=bucket, Key=key, Body=payload)

    resp = s3_client.get_object(Bucket=bucket, Key=key)
    sha_remote = hashlib.sha256(resp["Body"].read()).hexdigest()

    evidence_log({
        "test": "large_put_50mb",
        "iteration": iteration,
        "size_bytes": SIZE_50MB,
        "sha_local": sha_local,
        "sha_remote": sha_remote,
        "match": sha_local == sha_remote,
    })

    s3_client.delete_object(Bucket=bucket, Key=key)

    assert sha_local == sha_remote, "sha256 mismatch — partial write or corruption"
```

**NFR mapping**: NFR-2 (3/3 success rate 100%, sha256 IDENTICAL) — AC-3 보강.

### §6.5 `tests/spike/test_nas_minio_restart_idempotency.py` — Q2 manual gate

```python
# tests/spike/test_nas_minio_restart_idempotency.py
"""PoC 4 — Restart idempotency (Q2: manual DSM Container Manager UI STOP/START)."""
import hashlib
import os
import time

import pytest

from tests.spike.conftest import skip_if_no_nas


@skip_if_no_nas
@pytest.mark.manual  # opt-in: pytest tests/spike -m manual
def test_restart_idempotency_manual_gate(s3_client, bucket, evidence_log):
    payload = os.urandom(1024)
    sha = hashlib.sha256(payload).hexdigest()
    key = "spike/restart/pre-restart.bin"

    s3_client.put_object(Bucket=bucket, Key=key, Body=payload)

    print("\n" + "=" * 60)
    print("[MANUAL GATE] DSM Container Manager UI에서 minio container")
    print("              STOP → 30s wait → START 수행")
    print("              (5min sleep 자동 진행, 사용자 자리비움 시 timeout fallback)")
    print("=" * 60)

    # Sleep 5min — user performs manual restart via DSM UI
    time.sleep(300)

    # Retry-and-recover (boto3 default 3 retries within fixture)
    t0 = time.perf_counter()
    resp = s3_client.get_object(Bucket=bucket, Key=key)
    recovery_ms = (time.perf_counter() - t0) * 1000
    sha_remote = hashlib.sha256(resp["Body"].read()).hexdigest()

    evidence_log({
        "test": "restart_idempotency",
        "recovery_ms": round(recovery_ms, 2),
        "sha_match": sha == sha_remote,
        "manual_gate_timeout_fallback": False,  # 사용자 자리비움 검증 X — manual 실행 가정
    })

    s3_client.delete_object(Bucket=bucket, Key=key)

    assert sha == sha_remote, "sha256 mismatch after restart — durability violation"
    assert recovery_ms < 300000, f"recovery too slow: {recovery_ms}ms (> 5min limit)"
```

**OpRiskArch 검증**: 5min `time.sleep(300)` 동안 사용자 manual gate 의 운영 위험 평가
- 사용자 자리비움 시 5min timeout 후 자동 진행 — restart 미수행 상태로 GET 호출 → recovery_ms = 정상값 (restart 안 일어남), assertion PASS 하지만 **실제 idempotency 검증은 되지 않음**.
- 완화: evidence pack 에 `manual_gate_timeout_fallback` flag 박제 — MCT-149 ADR-027 D8 evidence 인용 시 사용자가 명시적으로 manual gate 수행 여부 추가 박제 의무.
- 본 Story 는 manual `pytest -m manual` opt-in marker → CI / 일상 test 에서 실수 실행 0 (pytest collection 자동 제외).

**NFR mapping**: NFR-3 (recovery ≤ 5min, boto3 default retry 충분) — AC-4 충족.

### §6.6 `tests/spike/test_nas_minio_partial_visibility.py` — Q5 multi-thread race-window

```python
# tests/spike/test_nas_minio_partial_visibility.py
"""PoC 5 — Partial object visibility (Q5: multi-thread, S3 atomic invariant)."""
import os
import threading
import time

import botocore.exceptions

from tests.spike.conftest import skip_if_no_nas

SIZE_50MB = 50 * 1024 * 1024


@skip_if_no_nas
def test_partial_visibility_atomic(s3_client, bucket, evidence_log):
    payload = os.urandom(SIZE_50MB)
    key = "spike/partial/object.bin"

    put_done = threading.Event()
    partial_results: list[tuple[str, object]] = []

    def put_thread():
        s3_client.put_object(Bucket=bucket, Key=key, Body=payload)
        put_done.set()

    def get_thread():
        # PUT 진행 중 (100ms 후 시작) GET 시도 — race window 확보
        time.sleep(0.1)
        while not put_done.is_set():
            try:
                resp = s3_client.get_object(Bucket=bucket, Key=key)
                data = resp["Body"].read()
                partial_results.append(("GOT", len(data)))
            except botocore.exceptions.ClientError as e:
                partial_results.append(("ERROR", e.response["Error"]["Code"]))
            time.sleep(0.05)

    t1 = threading.Thread(target=put_thread)
    t2 = threading.Thread(target=get_thread)
    t1.start()
    t2.start()
    t1.join()
    t2.join()

    # 검증: GOT 인 경우 반드시 SIZE_50MB (atomic — partial bytes 0)
    # ERROR 인 경우 NoSuchKey (PUT 완료 전) — acceptable
    atomic_ok = all(
        (r[0] == "GOT" and r[1] == SIZE_50MB)
        or (r[0] == "ERROR" and r[1] == "NoSuchKey")
        for r in partial_results
    )

    evidence_log({
        "test": "partial_visibility",
        "n_get_attempts": len(partial_results),
        "results": partial_results,
        "atomic_invariant": atomic_ok,
        "size_50mb": SIZE_50MB,
    })

    s3_client.delete_object(Bucket=bucket, Key=key)

    assert atomic_ok, f"partial bytes detected — atomic invariant violated: {partial_results}"
```

**SecurityArch 검증 — atomic invariant**: ADR-017 의 write-then-rename atomic primitive 가 S3 PUT 의 application-layer atomic semantic 와 정합한지 정량 입증. NoSuchKey / SIZE_50MB 외 결과 (e.g., partial bytes 100MB ÷ 2 = 25MB) 발견 시 **invariant 위반 → AC-5 FAIL → MCT-149 ADR-027 D6 결정 반영 의무**.

**NFR mapping**: NFR-4 (0 row partial) — AC-5 충족.

### §6.7 `.gitignore` modify — `.tmp/` 추가

현재 `c:/workspace/mclayer/mctrader-data/.gitignore` 에 `.tmp/` 누락 → 추가 의무 (evidence pack file 의 commit 사고 방지):

```diff
 # local env files (credentials)
 .env
+
+# spike evidence packs / scratch (MCT-148)
+.tmp/
```

**SecurityArch 검증**: `.tmp/evidence-pack-MCT-148.md` 가 gitignored 확인 → git status 시 untracked 분류 → commit 0 보장 (evidence pack 의 raw log 가 credential mask 안 했어도 repo 노출 risk 0).

### §6.8 `tests/spike/__init__.py` — 빈 file

```python
# tests/spike/__init__.py
```

pytest collection + `from tests.spike.conftest import skip_if_no_nas` import resolution 위한 빈 module marker.

## §7 설계 서사 — Deputy 분담 + 채택/반박 trail

### §7.1 Mapper (CodebaseMapperAgent) — baseline 인용

- **인용**: `src/mctrader_data/compactor/minio_uploader.py` 의 boto3 client 패턴 (BUCKET_NAME='mctrader-market' 하드코딩, default retry config 미명시)
- **반영**: §6.1 conftest 의 `s3_client` fixture 가 동일 boto3 + bucket 명 reuse + `Config(retries={"max_attempts": 3, "mode": "standard"})` 명시 → MCT-150 hardening 의 retry policy 결정 starting point
- **반박 없음**: src 변경 0 — baseline pattern 그대로 reference

### §7.2 Refactor (RefactorAgent) — N/A

- **scope**: spike Story (phase1_only), src/ 무변경 의무
- **결정**: refactor 항목 0 — 모든 신규 파일 `tests/spike/` 한정, 기존 코드 결합도 변경 0
- **MCT-150 forward-ref**: 본 spike 의 boto3 fixture 패턴이 MCT-150 단계에서 `minio_uploader.py` 의 retry/multipart config 통합 refactor target — 본 Story 비대상

### §7.3 SecurityArch (SecurityArchitectAgent) — 7.1~7.3 / 7.5~7.7 변호

- **§7.1 Trust boundary**: NAS endpoint = LAN 내부망 (192.168.50.200), 외부 노출 0. test fixture 환경변수 (`NAS_MINIO_*`) = 사용자 machine 의 shell env 만 — repo / CI / production secret store 와 격리.
- **§7.2 Threat model**:
  - T1 (credential leak via evidence pack): partial visibility test 의 raw log 에 access key 포함 risk → boto3 default 로그는 credential mask, evidence_log fixture 가 dict serialize 만 (key/secret 직접 호출 0) → mitigated.
  - T2 (production endpoint 침범): test fixture 의 `NAS_MINIO_*` 환경변수가 hot path `MINIO_ENDPOINT` 와 namespace 분리 → s3_client fixture 가 NAS endpoint 만 — production minio 침범 0.
  - T3 (gitignored 누락): `.tmp/evidence-pack-MCT-148.md` 가 `.gitignore` 매치 안되면 commit 사고 가능 → §6.7 에서 `.tmp/` 추가 의무 박제.
- **§7.3 Auth model**: MinIO root credential = LAN test 한정 (production 의 IAM separation 은 Stage 2 / MCT-155 scope).
- **§7.5 Data classification**: random bytes (`os.urandom`) — 실 시장 데이터 0, PII 0. evidence pack 의 raw log 박제 시 credential 노출 risk 0 (boto3 default mask).
- **§7.6 Audit trail**: evidence_log fixture 의 ISO-8601 timestamp + sha256 박제 → MCT-149 ADR-027 evidence 인용 시 reproducibility 보장.
- **§7.7 (해당 없음)**: production deploy / IAM rotation 등 Stage 2 scope.

### §7.4 OpRiskArch (OperationalRiskArchitectAgent) — 운영 리스크 5 항목

- **OR-1 (DR — Disaster Recovery)**: NAS local volume 의 durability — restart idempotency test 가 정량 확인 (사전 PUT object 가 restart 후 보존). **CONDITIONAL N/A** for cross-DC DR (Stage 1 LAN 한정).
- **OR-2 (Disconnect — Network resilience)**: boto3 default 3회 exponential backoff retry 의 충분성 — restart idempotency test 가 검증 actor. recovery_ms 측정 → MCT-150 retry policy 결정 input.
- **OR-3 (Clock — 시각 동기화)**: **CONDITIONAL N/A** — 본 spike 는 wall-clock latency 측정만, NAS NTP sync 의존 0 (test machine 의 perf_counter monotonic 사용).
- **OR-4 (Rate-limit)**: **CONDITIONAL N/A** — Stage 1 spike 의 120 PUT (latency) + 1 PUT (large) + N PUT (partial) 총 ~125 PUT, MinIO rate limit 충분 헤드룸 (production 부하 가정 X).
- **OR-5 (Env-isolation)**: §7.3 T2 와 동일 — namespace 격리 강제.
- **추가 우려 (manual gate 운영 위험)**: §6.5 의 5min `time.sleep(300)` 동안 사용자 자리비움 시 timeout fallback → restart 미수행으로 assertion 통과하지만 **실제 검증 0**. 완화: evidence pack `manual_gate_timeout_fallback` flag + MCT-149 ADR-027 evidence 인용 시 explicit 사용자 박제 의무.

### §7.5 TestContractArch (TestContractArchitectAgent, chief contributor) — §8 author

§8 sub-section 직접 author (아래 §8 본문 참조). §6.0 의 Q3/Q4/Q5 결정 trail 정합.

### §7.6 DataMigrationArch (DataMigrationArchitectAgent) — N/A

- **scope**: spike Story 는 schema 변경 0, migration 0, rollback 0
- **결정**: §11 항목 모두 N/A — 본 spike 는 read-only baseline measurement (사후 cleanup `delete_object` 포함, NAS bucket 의 state 변화 0)
- **idempotency consult (§11.6)**: restart idempotency test 가 sha256 roundtrip 으로 정량 입증 — DataMigrationArch primary scope X (compactor write semantic 변경 0)

## §8 Test Contract

### §8.1 단위 테스트

해당 없음 (spike Story — pytest collection 은 `tests/spike/` 한정).

### §8.2 통합 테스트 (5 PoC test)

| T-ID | 파일 | PASS criteria | Measurement |
|------|------|---------------|-------------|
| T1 | `test_nas_minio_http_health.py` | `urllib` HTTP GET → status 200 (live + ready 두 endpoint) | HTTP status code, response time |
| T2 | `test_nas_minio_latency_baseline.py` | 4 size × 30 PUT, p99 < limits per size (1KB:500ms / 1MB:1s / 10MB:3s / 50MB:10s) | p50/p95/p99 (ms) per size, min/max, full sample array |
| T3 | `test_nas_minio_large_put_50mb.py` | 50MB × 3 iter, sha_local == sha_remote 모두 True | sha256 hex (local + remote), match bool per iter |
| T4 | `test_nas_minio_restart_idempotency.py` | manual gate 후 GET 성공 + sha256 IDENTICAL + recovery_ms < 300000 | recovery_ms, sha_match bool, manual_gate_timeout_fallback flag |
| T5 | `test_nas_minio_partial_visibility.py` | 모든 GET attempt 가 (GOT, SIZE_50MB) 또는 (ERROR, NoSuchKey) — atomic_invariant True | n_get_attempts, results array, atomic_invariant bool |

**Boundary conditions (TestContractArch)**:
- T2 multipart threshold: 50MB > 5MB boto3 default → multipart 자동 분할 → 측정값에 자연스럽게 반영
- T5 race-window 확보 실패 (PUT 가 < 0.1s 인 경우, 1KB 라면 가능 but 50MB 는 LAN gigabit 기준 ~5s → race window 충분)
- T4 manual gate timeout: 사용자 자리비움 시 restart 0 → assertion 통과하지만 실제 검증 X → evidence flag 박제

**Invariants**:
- I-1 (sha256 roundtrip): T3 의 모든 iter + T4 의 restart 후 모두 sha256 IDENTICAL
- I-2 (S3 atomic): T5 의 모든 GET attempt 가 partial bytes 0 (GOT==SIZE_50MB or ERROR==NoSuchKey)
- I-3 (cleanup): 모든 test 가 `delete_object` 로 bucket state 복원 (NAS 누적 0)

### §8.3 보안 테스트

| ST-ID | 검증 항목 | 방법 |
|-------|-----------|------|
| ST1 | NAS_MINIO_* 환경변수 가 .env / settings.json / hardcoded 어디에도 없음 | `grep -r "NAS_MINIO" src/ tests/` → conftest.py `os.environ.get` 호출 외 0 hit |
| ST2 | `.tmp/evidence-pack-MCT-148.md` 가 `.gitignore` 매치 | `.gitignore` 에 `.tmp/` 추가 + `git check-ignore .tmp/evidence-pack-MCT-148.md` PASS |
| ST3 | credential 평문 노출 0 | evidence pack 의 raw log 에 access key / secret 직접 호출 0 (boto3 default mask 의존) |

### §8.4 Performance Baseline (본 Story 가 baseline 박제 source)

| 항목 | 기대값 (LAN gigabit) | 측정 위치 |
|------|---------------------|----------|
| 1KB PUT p99 | < 500ms | T2 evidence (size=1KB) |
| 1MB PUT p99 | < 1s | T2 evidence (size=1MB) |
| 10MB PUT p99 | < 3s | T2 evidence (size=10MB) |
| 50MB PUT p99 | < 10s | T2 evidence (size=50MB) |
| 50MB roundtrip success rate | 3/3 (100%) | T3 evidence |
| Restart recovery | < 5min | T4 evidence |
| Partial visibility violations | 0 | T5 evidence |

본 baseline 값은 MCT-149 ADR-027 D8 (Spike scope) evidence + MCT-150 production hardening 의 retry/multipart config 결정 input.

### §8.5 Long-running / stateful / background — `§8.5_active = false`

§8.5.0 4 조건 평가 (CFP-378 AC-5):
- Long-running connection: N — boto3 single PUT/GET, persistent connection 없음
- Stateful in-memory cache: N — test 의 fixture 는 stateless session-scope client 만
- Background worker / queue: N — main thread + 1 helper thread (T5) 한정, queue 0
- Process restart-aware: N — T4 가 NAS 측 container restart 측정이지만 본 test process 는 restart 0

→ 4개 모두 N → `§8.5_active = false`. TestContractArch dissent 0.

## §9 Impl Manifest

| File | Action | Owner |
|------|--------|-------|
| `tests/spike/__init__.py` | 신규 (빈 file) | DeveloperAgent |
| `tests/spike/conftest.py` | 신규 (NAS endpoint + evidence_log fixture, marker registration) | DeveloperAgent |
| `tests/spike/test_nas_minio_http_health.py` | 신규 (T1) | DeveloperAgent |
| `tests/spike/test_nas_minio_latency_baseline.py` | 신규 (T2) | DeveloperAgent |
| `tests/spike/test_nas_minio_large_put_50mb.py` | 신규 (T3) | DeveloperAgent |
| `tests/spike/test_nas_minio_restart_idempotency.py` | 신규 (T4, `pytest -m manual` opt-in) | DeveloperAgent |
| `tests/spike/test_nas_minio_partial_visibility.py` | 신규 (T5) | DeveloperAgent |
| `.gitignore` | modify (`.tmp/` 추가) | DeveloperAgent |
| `docs/stories/MCT-148.md` | append (§6~§11) | ArchitectPLAgent (본 단계) → 후속 lane append |
| `.tmp/evidence-pack-MCT-148.md` | 신규 (테스트 실행 시 evidence_log fixture 가 생성, gitignored) | QADeveloperAgent (실행 시점) |

**Pytest 실행 명령**:
- 일상 (T1~T3 + T5): `pytest tests/spike/`
- T4 포함 manual: `pytest tests/spike/ -m manual` 또는 `pytest tests/spike/test_nas_minio_restart_idempotency.py -v`
- 환경변수 없을 때: 자동 SKIP (5 test 모두 `@skip_if_no_nas`)

## §10 FIX Ledger

(initial empty — Orchestrator 가 FIX 발생 시 append)

| FIX# | Lane | Root cause | Resolution | Timestamp |
|------|------|------------|------------|-----------|

## §11 PMO retro (placeholder)

PMOAgent 가 Story 완료 (구현 + evidence pack 박제 + MCT-149 진입) 후 append. 본 spike 의 measurement 값 + MCT-149 ADR-027 D8 transcription 정합 + manual gate operational learning 박제 의무.

---

**§6~§11 author 완료 (ArchitectPLAgent, 2026-05-12).** Deputy 7 (Mapper / Refactor N/A / SecurityArch / OpRiskArch / TestContractArch chief / DataMigrationArch N/A / ArchitectAgent chief) 통합 + Q2/Q3 사용자 답변 + Q4/Q5 ArchitectPL 결정 반영. Story phase 전환은 Orchestrator scope (`phase:요구사항` → `phase:설계` → `phase:구현`).
