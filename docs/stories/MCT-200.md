---
story_key: MCT-200
story_scope: data
story_issues:
  - repo: mclayer/mctrader-data
    number: 97
    url: https://github.com/mclayer/mctrader-data/issues/97
status: phase:설계
parent_epic: 단독 Story (Epic 미생성 — 운영 인시던트 단독 incident response)
created_at: 2026-05-17
delegates: []
adr_carrier: mctrader-hub:docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md (Amendment 3, Proposed)
adr_status: TBD (Phase 1 spec 단계 ArchitectAgent draft → 사용자 verdict)
upstream_story: []
downstream_story:
  - Story 2 (별 Epic 위탁) — silent-skip fail-fast 코드 fix (`src/mctrader_data/compactor/l2.py` `_compact_hour_nas` return None → raise + Counter emit), ADR-027 Amendment 3 Accepted 후 진입
related:
  - "#48 MCT-159 compactor L1 backlog cleanup (orderbooksnapshot 분 부분 해소, post-LAND 정량 검증)"
cross_repo_isolation: cross-repo 1건 (mctrader-hub ADR-027 Amendment 3 + domain-knowledge/data-health/minio-bucket-policy-iam.md edit)
worktree: .claude/worktrees/mct-200
branch: worktree-mct-200
cutoff_classification: "full-lane Story (강제 대상 — 신규 ADR Amendment + 도메인 모델 추가)"
cross_story_pattern_threshold: REACHED (N=3, silent-skip Amendment 시리즈 = MCT-160 + MCT-164 + MCT-200, ADR-045 Amendment 5 §D-9)
spec: docs/superpowers/specs/2026-05-17-mct-200-minio-iam-ws-a-backfill-design.md
§8.5_active: true (TestContractArch Phase 1.0 평가 확정 — MinIO container restart 후 IAM policy 잔존 + access-key 잔존 + volume mount 검증 영역 — Researcher Unknown #1 회귀 차단 의무)
live_touching: true (production MinIO bucket IAM credential 변경 → LiveOpsDeputy ACTIVE, 7 deputy spawn)
deputy_spawn: 6 permanent + LiveOps CONDITIONAL (LiveOrdering 비활성 — read-only backfill, ordering side effect 0)
phase_0_5_blanket_debate: TBD (ArchitectPLAgent Phase 0.5 평가)
---

# MCT-200: MinIO bucket policy / IAM 복원 + silent-skip ADR draft + WS-A 117GB 백필 (운영 인시던트 대응)

- **Issue**: [mclayer/mctrader-data#97](https://github.com/mclayer/mctrader-data/issues/97)
- **Spec**: [docs/superpowers/specs/2026-05-17-mct-200-minio-iam-ws-a-backfill-design.md](../superpowers/specs/2026-05-17-mct-200-minio-iam-ws-a-backfill-design.md)
- **ADR**: [mctrader-hub:docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md](https://github.com/mclayer/mctrader-hub/blob/main/docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md) (Amendment 3, Proposed)
- **Parallelism phase**: Phase 1 순차 → Phase 2 3-group 병렬 (A IAM → B WS-A 순차 prerequisite, C ADR/docs 병렬)
- **Live touching**: ✅ (LiveOpsDeputy ACTIVE — production MinIO IAM credential)
- **Deputy spawn**: 7 (6 permanent + LiveOps CONDITIONAL)

## §1 사용자 요구사항 (brainstorm spec §1 verbatim + 사용자 결정 ledger)

> mctrader-compactor 가 MinIO 버킷 `mctrader-market` (`http://192.168.50.200:9000`) 에 보내는 `_list_objects` / `head_object` 호출이 **전 키프리픽스에서 403 Forbidden** 응답. 이게 117GB L1 누적의 직접 원인 한 축: forward `_run_l2_for_parquet → L2Compactor._compact_hour_nas` 가 `_list_objects` 403 → 예외 catch → silent `return None` → forward L1→L2 promotion 무한 silent failure → `/market/orderbooksnapshot` **135GB tier=L1 누적** (date 2026-05-13 ~ 05-17, 23,981 files). put_streaming (NAS PUT) 자체는 간헐적으로 성공 → `s3:PutObject` 는 허용되나 `s3:ListBucket` + `s3:HeadObject` 가 거부 = IAM/policy 회귀.

**사용자 결정 ledger (brainstorming 2 dialog)**:
- silent-skip ADR draft: **본 Story 내 ADR draft 포함** (Proposed status). 코드 fix 는 별 Epic 위탁.
- 117GB 회수 (WS-A historical promotion): **AC 포함**. IAM 복원 후 operator 수동 `mctrader-data cli promote-historical 2026-05-13~15 upbit orderbooksnapshot` 실행 + 16,946 files L1→L2 승급 verify.
- ADR carrier: **ADR-027 Amendment 3 (mctrader-hub cross-repo)** — silent-skip Amendment 시리즈 단일 carrier (Amendment 1 MCT-160 cadence + Amendment 2 MCT-164 multi-channel sibling 연속).

**스코프 IN**: MinIO bucket `mctrader-market` policy/IAM 복원 (s3:ListBucket + s3:GetObject + s3:HeadObject + s3:PutObject 최소 권한 부여), ADR-027 Amendment 3 Proposed draft, `mctrader-hub:docs/domain-knowledge/domain/data-health/minio-bucket-policy-iam.md` 신규, WS-A operator 수동 백필 (16,946 files).

**스코프 OUT**: `src/**` 코드 변경 (silent-skip fail-fast 는 별 Epic Story 2 위탁), `l2.py` byte-order 이슈 B 별 Story.

## §2 근본 원인 (brainstorm spec §2 verbatim)

| RC | 내용 | 증거 |
|----|------|------|
| RC-1 | MinIO bucket `mctrader-market` 운영 자격증명에 `s3:ListBucket` + `s3:HeadObject` 권한 부재 / 거부 | ClientError 로그 (operation=HeadObject status=403 / operation=ListObjectsV2 status=403); PUT 로그는 성공 — IAM action 비대칭 |
| RC-2 | `L2Compactor._compact_hour_nas` (`src/mctrader_data/compactor/l2.py:60-66, 137-176`) 의 `_list_objects` 예외 catch → silent `return None` → forward L1→L2 무한 silent failure | CLAUDE.md §Compactor source 분기 규약 ("silent skip 금지 — ADR-027 Amendment 1 정합") + 사용자 로그 "[L2Compactor] NAS _list_objects failed prefix=... — skip (INV-3)" |
| RC-3 | 의심 회귀 commit `af62570` 변경 = docker/minio/compose + .env 재배치 + scripts/ha/* + tools/. **MinIO bucket policy / IAM 정의 파일 0**. → runtime config drift 가능성 (root credentials drift / ephemeral volume reset / image tag default) | `git show af62570 --stat` = 16 files +1,251 / -0, policy JSON 0건 |

**Cross-Story silent-skip pattern (N=3 도달, ADR-045 Amendment 5 §D-9)**:
- MCT-160 (cadence trigger) — ADR-027 Amendment 1
- MCT-164 (multi-channel source) — ADR-027 Amendment 2
- **MCT-200** (NAS-side LIST/HEAD) — **ADR-027 Amendment 3 draft (본 Story)**

`adr_draft_emitted` escalation default 활성 → 사용자 결정 1 ("본 Story 내 ADR draft 포함") 정합.

## §3 scope / Change Plan

ArchitectAgent chief author + 7 deputy 통합 산출. **코드 변경 0** (silent-skip fail-fast 코드 fix 는 별 Epic Story 2 위탁, 본 Story OUT). 본 Story 의 산출 = (a) MinIO IAM 복원 운영 artifact + (b) ADR-027 Amendment 3 Proposed draft (mctrader-hub cross-repo) + (c) domain-knowledge minio-bucket-policy-iam.md (mctrader-hub cross-repo) + (d) bucket policy JSON SSOT 4종 + (e) WS-A 16,946 files 백필 operator runbook + verify gate.

### §3.1 7 deputy 산출 통합

- deputy 1: **CodebaseMapperAgent** (fact-only, 추론 금지) — `compactor/l2.py:162-173` `_compact_hour_nas` silent-skip site / `nas_uploader.py:571-593` `_list_objects` (raise but caller catch) / `nas_uploader.py:396-418` `head_object` 4-tuple verify primitive / `dual_writer.py:60-65` DualWriteResult enum 3종 frozen / `compactor/runner.py:174-199, 226-247, 148-152, 324-358, 439-475, 478-510` forward + WS-A 호출 surface / `cli.py:898, 963-987` `promote-historical` subcommand 등록 + NAS env 분기 / `mctrader-hub:ADR-027` Amendment trail line 29-42 + Amendment 2 body line 190-213 + History line 642-651 (Amendment 3 박제 위치 = line 41 아래 + line 651 파일 끝). 변경 영향 map: 본 Story 코드 변경 0 → 본 Story 영향 X, Story 2 downstream 만 영향.
- deputy 2: **RefactorAgent** — ADR-027 Amendment 3 fail-fast interface contract 5종 권고 (Counter `mctrader_data_compactor_nas_403_total{op,action}` + Counter `mctrader_data_nas_uploader_idempotent_skip_total{tier,channel}` + RuntimeError raise + Prometheus alert `NASCompactorListHead403` for=0m + Story 2 Epic seed). bucket policy JSON SSOT 4-file 분리 advocacy (read/write/list/admin) — T-B1 CRITICAL 완화. verify script 분리 (`verify_minio_iam_restore.py` + `verify_ws_a_backfill_mct200.py`). runbook ↔ script 분리 (runbook=판단층 / script=executor / JSON=port contract).
- deputy 3: **SecurityArchitectAgent** — 5 Tier trust boundary (Operator → host volume → `.env` → minio-init bootstrap → S3 API) + 3 INV-S + STRIDE 13건 (CRITICAL/HIGH 9건 — T-B1 bucket policy git-tracked SSOT 부재 / T-B2 minio-init bootstrap 미포함 / T-B3 ephemeral volume IAM state 손실 / T-A2~T-A5 / T-X1 / T-S1). 5 적극 이의: T-B1 → minio-bucket-policy-iam.md 신규 P0 / T-B3 → policy + svcacct snapshot 양쪽 SSOT 필수 / AC-1 4 action allow + N action deny round-trip / AC-5 IAM drift detect cron / T-X1 ADR Amendment 3 P0.
- deputy 4: **OperationalRiskArchitectAgent** (§7.4 primary + §11.6 idempotency consult) — DR (`restart: unless-stopped` KEEP + bucket policy ephemeral 검증 Phase 2 1st step gate) / disconnect (timeout vs 403 distinct + boto3 `max_attempts=3 mode="adaptive"` metadata op 한정 + circuit breaker 5분 sliding window N=10 → 30분 OPEN) / rate limit (Synology HDD ~100 PUT/s + WS-A `--max-concurrency=2` cap) / env isolation (단일 production endpoint + IP allowlist + `.env` 0600 + 90d rotation) / **clock sync HIGH ACTIVE** (NTP source 동일화 + drift ≤ 1s + `node_timex_offset_seconds` alert) / health check (`/minio/health/ready` 추가 + compactor startup probe + 5분 cron probe + `mctrader_data_nas_iam_health` Gauge). §11.6 access-key rotation = **blue-green 패턴** 의무 (direct update = in-flight PUT fail).
- deputy 5: **TestContractArchitectAgent** (§8 + §8.5_active=true 평가 확정) — 8 verify gate invariants (§8) + 4 stateful invariant (§8.5.1 long-running 24h / §8.5.2 restart recovery **CRITICAL** Researcher Unknown #1 차단 / §8.5.3 idempotency replay / §8.5.4 volume mount 검증). dissent 0 (PL Phase 1.0 평가 ↔ SubAgent 일치).
- deputy 6: **DataMigrationArchitectAgent** (§11 primary) — bucket policy idempotent restore (mc admin policy add re-entry safe + diff 비교) / WS-A 백필 5 INV (CLAUDE.md §backfill mode) + 4 INV-A/B/C/D (CLAUDE.md §historical tier promotion) / rollback 3 sub (정책 부분 복원 / WS-A 부분 실패 / ADR Proposed→Story 2 진입 rollback) / data integrity 6 invariant / schema evolution 0. 3 적극 이의: manifest write ↔ sentinel write atomicity HIGH (CodebaseMapper 위탁) / access-key rotation Point of no return MEDIUM / JSON SSOT git tree 갱신 누락 LOW.
- deputy 7: **LiveOpsDeputyAgent** (§13 primary, CONDITIONAL ACTIVE — production MinIO IAM credential 변경) — 2-eyes approval + IAM 복원 kill switch ordering + 2 Runbook outline (minio-bucket-policy-iam-restore.md + ws-a-historical-promotion-operator.md) + IAM credential lifecycle (revoke+recreate / 90일 / vault migration 별 Epic / audit log) + incident response (Issue #97 SSOT + post-mortem 의무) + production rollout 가시성.

### §3.2 Phase 1 산출물 list (본 PR scope)

| Path | 영역 | 작성자 | repo |
|------|------|--------|------|
| `docs/stories/MCT-200.md` (본 file §3/§7/§8/§8.5/§11/§13) | Story file integrated | ArchitectAgent chief author | mctrader-data |
| `.architect-output/ADR-027-amendment-3-draft.md` | ADR Proposed draft (cross-repo carrier) | ArchitectAgent | mctrader-data (worktree 박제 → Phase 2 Group C mctrader-hub PR) |
| `.architect-output/domain-knowledge-minio-bucket-policy-iam.md` | domain-knowledge SSOT (cross-repo carrier) | ArchitectAgent + SecurityArch + OpRisk | mctrader-data (worktree 박제 → Phase 2 Group C mctrader-hub PR) |
| `scripts/minio-policies/read.json` | bucket policy SSOT — reader | ArchitectAgent | mctrader-data |
| `scripts/minio-policies/write.json` | bucket policy SSOT — ingestion-only | ArchitectAgent | mctrader-data |
| `scripts/minio-policies/list.json` | bucket policy SSOT — compactor _list_objects | ArchitectAgent | mctrader-data |
| `scripts/minio-policies/admin.json` | bucket policy SSOT — 4 action 최소권한 (RC-1 복원 대상) | ArchitectAgent | mctrader-data |

### §3.3 Phase 2 3-group 병렬 결정 (file path disjoint)

| Group | Owner | files | prerequisite | parallel |
|-------|-------|-------|--------------|----------|
| **A IAM 복원** | InfraEngineerAgent | `scripts/restore_minio_iam.sh` (Python 권고 — wal_freeze.py 패턴 정합), `scripts/verify_minio_iam_restore.py` (4 action round-trip + N action deny), `docs/runbooks/minio-bucket-policy-iam-restore.md`, `docs/audit/MCT-200-minio-iam-pre-restore-snapshot.md` | Phase 2 1st step = mc admin policy info + docker exec env 직접 검증 (Edge-RC2 완화) | Group C 와 병렬 |
| **B WS-A 백필** | DataEngineerAgent | `docs/runbooks/ws-a-historical-promotion-operator.md`, `scripts/verify_ws_a_backfill_mct200.py` (MCT-173 D8=C pattern 재사용), `docs/audit/MCT-200-ws-a-backfill-verify-2026-05-13-15.md` | Group A LAND (INV-E IAM 선결) | Group C 와 병렬 |
| **C ADR + DK + CLAUDE.md** | ArchitectAgent + DataEngineerAgent | `mctrader-hub:docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md` (Amendment 3 edit, 3 영역), `mctrader-hub:docs/domain-knowledge/domain/data-health/minio-bucket-policy-iam.md` (신규), `mctrader-data:CLAUDE.md` (§MinIO bucket policy / IAM 신규 + §historical tier promotion INV-E 추가 + §관련 ADR Amendment 3 추가) | none | Group A/B 와 병렬 |

### §3.4 cross-repo PR carrier 결정

Phase 2 Group C = **mctrader-hub cross-repo PR 1개** + **mctrader-data PR (본 worktree → main)**. mctrader-hub PR scope = ADR-027 Amendment 3 edit (3 line range — Amendment trail line 41 / Amendment body line 213 / History line 651) + domain-knowledge minio-bucket-policy-iam.md 신규. 본 Story 의 `.architect-output/` 박제 = Phase 2 Group C 시점 cross-repo PR 으로 transfer.

### §3.5 사용자 brainstorming 결정 정합

- 결정 1 (silent-skip ADR draft): ADR-027 Amendment 3 Proposed draft `.architect-output/` 박제 → Phase 2 Group C cross-repo PR. 코드 fix Story 2 별 Epic 위탁 보존.
- 결정 2 (117GB 회수): WS-A AC-4 포함 (operator 수동 `promote-historical 2026-05-13~15 upbit orderbooksnapshot`) + verify script + audit.
- 결정 3 (ADR carrier): ADR-027 Amendment 3 단일 carrier (mctrader-hub cross-repo). Amendment 1+2 sibling 시리즈 연속.

### §3.6 코드 변경 0 invariant

본 Story = `src/**` Write/Edit 0건. silent-skip fail-fast 코드 fix (`l2.py:162-173` `_compact_hour_nas` return None → raise) = ADR-027 Amendment 3 Accepted 후 별 Epic Story 2 진입. ADR-045 §결정 3 정합.

## §4 Acceptance Criteria (brainstorm spec §4 verbatim)

- **AC-1** (IAM 복원, RC-1): MinIO bucket `mctrader-market` 운영 자격증명으로 4 action (PUT/LIST/HEAD/GET) 모두 allow → `boto3.list_objects_v2(Bucket='mctrader-market', Prefix='l1/', MaxKeys=1)` 403 없이 키 ≥1 반환 + `head_object` 200 반환.
- **AC-2** (forward path 회복, RC-2): IAM 복원 후 6분 이내 `mctrader-compactor` 로그에 `dual-write OK tier=L2` 다수 출현 + `_list_objects failed` silent skip 로그 0건.
- **AC-3** (silent-skip 정책 박제): ADR-027 Amendment 3 Proposed draft 박제 + domain-knowledge minio-bucket-policy-iam.md SSOT 신규.
- **AC-4** (WS-A 117GB 백필 검증, 사용자 결정 2): operator 수동 실행 → 16,946 L1 files L2 승급 실측 (verify_ws_a_backfill_mct200.py PASS, ratio ≥ 0.90 partial-loss threshold MCT-173 D8=C 정합) + audit/backfill-manifest YAML 무변.
- **AC-5** (IAM 회귀 방지 감지): mc admin policy/user enumeration snapshot 박제 + 향후 IAM drift 감지 운영 알람 정의 (Prometheus rule 또는 runbook check — Phase 2 ArchitectAgent 결정).

## §5 Risk (brainstorm spec §5 verbatim)

- **Edge-RC1**: 부분 IAM 복원 (s3:HeadObject만 복원, s3:ListBucket 누락) — verify_minio_iam_restore.py 가 4 action 모두 round-trip smoke (PUT+LIST+HEAD+GET) 완화.
- **Edge-RC2**: 회귀 source = `af62570` commit 자체가 아닌 runtime config drift 가능 (Researcher Unknown #1) — code revert 무효 가능. **완화** = Phase 2 1st step = mc admin policy info + docker exec env 직접 검증 후 revert 결정.
- **Edge-RC3**: 117GB L1 누적 기간(~4일) intermediate L2/L3 segments 일부 committed — IAM 복원 후 중복 처리 vs 부분 손실. **완화** = WS-A `.compacted` sentinel + INV-2 already_promoted no-op (재실행 safe).
- **Risk-Pattern-N3**: silent-skip 누적 3건 (MCT-160+164+200) — ADR draft 만으로 4번째 차단 불가. **완화** = ADR-027 Amendment 3 에 fail-fast 코드 fix Epic seed 명시 (Story 2 downstream).
- **Risk-WS-A**: operator 수동 백필 중 OOM / 디스크 압박 재발 — **완화** = MCT-163 F6 iter_batches streaming + WS-B sweep 6분 cadence 자동 회수.
- **Risk-OUT-스코프 긴장**: 사용자 "코드 변경 OUT" 선언 vs ADR-027 §D6 silent-skip 위반 cross-ref — **완화** = ADR draft = Proposed (status only), 코드 fix 는 별 Epic 위탁.

## §6 scope_manifest

[brainstorm spec §6 참조 — Phase 1 PR open 시 Issue body 에 박제]

본 Story 의 planned_adrs / planned_files / planned_claude_md_sections / planned_domain_knowledge / planned_operational_artifacts SSOT 는 [spec §6](../superpowers/specs/2026-05-17-mct-200-minio-iam-ws-a-backfill-design.md#§6-scope_manifest-pmo-2nd-pass) 에 박제. ArchitectPLAgent Phase 1 통합 후 본 §6 에 final 박제.

## §7 보안 / DR / Idempotency

ArchitectAgent + SecurityArchitectAgent (§7.1-§7.3 / §7.5-§7.6) + OperationalRiskArchitectAgent (§7.4) + LiveOpsDeputy (§13 cross-ref) 통합.

### §7.1 Trust boundary 5 Tier (SecurityArch)

```
Tier A — Operator (2-eyes approval, LiveOps §13.1)
    ↓ ssh / docker exec
Tier B — host volume `/volume1/docker/minio/data/.minio.sys/config/iam/` (MinIO IAM state in-band SSOT)
    ↓ docker volume mount
Tier C — `.env` credential (boto3 process env only, INV-S1 단일 SSOT)
    ↓ docker-compose env_file injection
Tier D — minio-init bootstrap container (mc admin user/policy add 의무)
    ↓ S3 API call
Tier E — S3 API (HTTP port 9000, SigV4 15분 window)
```

### §7.2 3 INV-S (SecurityArch)

- **INV-S1** Tier-C `.env` 단일 SSOT (boto3 process env only). `compactor/minio_uploader.py:43-44` legacy default `mctrader` access_key + empty secret default = silent fallback 위험 (Story 2 별 Epic 위탁).
- **INV-S2** Tier-B host volume `/volume1/docker/minio/data/.minio.sys/` = MinIO IAM state in-band SSOT. 외부 versioned policy JSON 부재 = **T-B1 CRITICAL** (본 Story `scripts/minio-policies/*.json` 4종 + minio-bucket-policy-iam.md 으로 완화).
- **INV-S3** compactor (LIST/HEAD/GET) + collector/DualWriter (PUT) 동일 access_key 공유 (Tier-C 단일 SSOT). IAM action 비대칭 (mc admin policy 의 LIST/HEAD/GET 누락) = **RC-1 직접 증거**.

### §7.3 STRIDE 위협 13건 (SecurityArch — CRITICAL/HIGH 9건)

| ID | 위협 | severity | 완화 |
|----|------|----------|------|
| T-B1 | bucket policy JSON git-tracked SSOT 부재 (RC-3 회귀 source 후보 #1) | **CRITICAL** | `scripts/minio-policies/*.json` 4종 (본 Story 박제) + minio-bucket-policy-iam.md |
| T-B3 | ephemeral volume IAM state 손실 (현재 incident 시나리오 정확 일치) | **CRITICAL** | 정책 JSON + svcacct list YAML 양쪽 SSOT + Phase 2 1st step volume mount 검증 (§7.4) |
| T-B2 | minio-init bootstrap `restart: "no"` + mc admin user/policy add 미포함 → 재배포 시 IAM 회귀 silent | **CRITICAL** | Phase 2 Group A bootstrap container 의무화 (별 PR seed) |
| T-A2 | mc admin policy mutation tampering (operator workstation malware) | HIGH | 2-eyes approval (LiveOps §13.1) + audit log append-only |
| T-A3 | mc admin audit trail 부재 (af62570 회귀 source 추정 어려움 증거) | HIGH | audit log `docs/audit/MCT-200-iam-rotation-log.md` 영구 보존 (Phase 2 `/var/lib/mctrader/audit/`) |
| T-A4 | mc admin user info stdout 평문 secret 누설 | HIGH | `--json` redirect 0600 + `> /dev/null` 의무 (runbook) |
| T-A5 | root credential 그대로 재사용 (svcacct 미사용, 권한 분리 무력화) | HIGH | svcacct 의무화 (minio-bucket-policy-iam.md §3.1) |
| T-X1 | compactor silent-skip (RC-2) | HIGH | **ADR-027 Amendment 3 Proposed (본 Story carrier)** → Story 2 코드 fix |
| T-S1 | HTTP MITM SigV4 replay (15분 window) | MEDIUM | IP allowlist `aws:SourceIp` Condition + HTTPS 전환 별 Story |
| T-A1 | bucket public read 가능성 | LOW | read.json 명시적 SA 한정 |
| T-A6 | mc alias 자격증명 평문 `~/.mc/config.json` | LOW | host file 0600 + workstation 격리 |
| T-S2 | log injection (prefix=...) | LOW | structured logging (JSON) + key whitelist |
| T-Replay | SigV4 replay outside 15분 | INFO | MinIO default 검증 |

### §7.4 OperationalRiskArchitectAgent — 운영 리스크 (primary)

#### §7.4.1 DR (Disaster Recovery)

- `restart: unless-stopped` **KEEP** (현 compose.yml 정합).
- **Phase 2 1st step = bucket policy persistence 검증 gate** (Edge-RC2 완화):
  ```bash
  docker exec mctrader-minio ls /data/.minio.sys/config/iam/
  # 디렉터리 존재 + 정책 파일 존재 → host volume persistent
  # 디렉터리 부재 → ephemeral volume = T-B3 CRITICAL 시나리오 확정
  ```
- `.env` host backup 일 1회 + RPO 메타 시간 단위.

#### §7.4.2 Disconnect handling

- `_list_objects` timeout vs 403 distinct error handling 분리 의무 (ADR Amendment 3 권고 — RuntimeError raise 시 status_code metadata 보존).
- boto3 `max_attempts=1 mode="standard"` → `max_attempts=3 mode="adaptive"` 권고 (**metadata op 한정** — multipart upload 는 `max_attempts=1` KEEP, sha256 ≠ ETag invariant ADR-027 §D6 정합).
- **Circuit breaker** 5분 sliding window N=10 fail → 30분 OPEN (Prometheus Counter `mctrader_data_compactor_nas_403_total{action="fail_fast"}` 기반).

#### §7.4.3 Rate limit

- Synology HDD bound ~100 PUT/s sequential.
- WS-A `--max-concurrency=2` cap 권고 (default 1 + spike 2 — forward path 충돌 회피).
- 평시 forward path PUT rate baseline 의무 (DataMigrationArch consult).

#### §7.4.4 Env isolation

- 단일 production endpoint `192.168.50.200:9000` (별 dev/test 부재).
- IP allowlist `aws:SourceIp` Condition (mc admin policy 의 Condition 절).
- `.env` 0600 + 90일 rotation (D2 amend 정합).
- Runtime env-based credential injection KEEP (vault migration 별 Epic — LiveOps §13.4).

#### §7.4.5 Clock sync (**HIGH ACTIVE**)

- compactor container ↔ NAS DSM NTP source 동일화 의무.
- Drift ≤ 1s tolerance.
- `node_timex_offset_seconds` alert: warn > 1s, crit > 30s.
- 위반 시 SigV4 timestamp 검증 실패 → 403 (T-Replay sibling).

#### §7.4.6 Health check

- MinIO `/minio/health/live` KEEP.
- `/minio/health/ready` 추가 권고 (Phase 2 Group A).
- compactor startup probe: `_list_objects(MaxKeys=1)` + `head_object` (RC-1 회귀 즉시 감지).
- 5분 cadence cron probe + `mctrader_data_nas_iam_health` Gauge.

### §7.5 민감 데이터 분류 (SecurityArch INV-D1/D2/D3)

| 데이터 | 분류 | 평문 출력 | 보존 | 위치 |
|--------|------|----------|------|------|
| `MINIO_ROOT_USER/PASSWORD` | Secret | 0 (NEVER stdout) | 영구 + 90d rotate | host `.env` 0600 |
| svcacct access-key / secret-key | Confidential / Secret | 0 (NEVER git/log/stdout) | 90d rotation | `.env` runtime injection |
| bucket policy JSON | Internal | git diff PR | 영구 (versioned) | `scripts/minio-policies/*.json` SSOT |
| audit log (rotation) | Confidential | git diff PR | 1년 (INV-D3) | `docs/audit/MCT-200-iam-rotation-log.md` (Phase 2 `/var/lib/mctrader/audit/` 영속) |
| pre-restore snapshot | Internal | git diff PR | 영구 | `docs/audit/MCT-200-minio-iam-pre-restore-snapshot.md` |

### §7.6 Operational Risk 10건 통합표 (OpRisk + SecurityArch + DataMigration cross-ref)

| ID | Risk | severity | 완화 |
|----|------|----------|------|
| R1 | bucket policy ephemeral volume reset | CRITICAL | §7.4.1 Phase 2 1st step 검증 gate |
| R2 | 부분 IAM 복원 (Edge-RC1) | HIGH | §11.4 atomic restore + 4 action allow + N action deny round-trip |
| R3 | IAM drift 재발 silent | HIGH | mc admin policy info + user list snapshot vs SSOT diff cron (AC-5) |
| R4 | WS-A alarm storm cascade | HIGH | Prometheus alert grouping + WS-B sweep 자동 회수 |
| R5 | WS-A spike vs forward PUT 충돌 | HIGH | §7.4.3 `--max-concurrency=2` cap |
| R6 | clock drift (CONDITIONAL ACTIVE → HIGH ACTIVE) | HIGH | §7.4.5 NTP 동일화 + alert |
| R7 | `.env` 평문 누출 | MEDIUM | §7.5 INV-D1 + chmod 0600 |
| R8 | access-key rotation Point of no return | MEDIUM | §11.6 blue-green pattern 의무 |
| R9 | timeout vs 403 silent skip | MEDIUM | §7.4.2 distinct error handling (ADR Amendment 3) |
| R10 | Synology RAID degrade silent | LOW | DSM health monitoring 별 Story |

### §7.7 적극 이의 통합 (SecurityArch 5건 + OpRisk 4건 + DataMigration 3건)

- **adopted (P0 차단 사유 적용)**: T-B1 bucket policy JSON SSOT 박제 (§3.2) / T-B3 정책 + svcacct YAML 양쪽 SSOT (§7.4.1) / AC-1 4 action + N action deny round-trip 보완 (§7.4.2) / AC-5 mc admin drift detect cron (§7.4 + §11.4) / T-X1 ADR Amendment 3 P0 (§3.4 cross-repo carrier) / Phase 2 1st step volume mount 검증 gate (§7.4.1) / boto3 max_attempts adaptive metadata op 한정 (§7.4.2) / WS-A `--max-concurrency=2` cap (§7.4.3) / clock sync HIGH ACTIVE (§7.4.5) / manifest write ↔ sentinel write atomicity HIGH (CodebaseMapper Story 2 위탁 명시, §11) / access-key rotation blue-green Point of no return MEDIUM (§11.6) / JSON SSOT git diff PASS 후 `--execute` runbook 강제 (§13).
- **rejected**: 없음 (모든 적극 이의 P0/P1 채택, 본 Story 결정 차원 정합).

## §8 Test Contract

TestContractArchitectAgent self-write (dissent 0 — PL Phase 1.0 §8.5_active=true 평가 ↔ SubAgent 분석 일치).

### §8.1 verify gate 8 invariants (AC mapping)

| inv | 이름 | AC | 검증 도구 | green 조건 | 비고 |
|-----|------|----|-----------|-----------|------|
| INV-T1 | IAM 4 action round-trip allow | AC-1 | `scripts/verify_minio_iam_restore.py` | PUT/LIST/HEAD/GET 4/4 200 | wal_freeze.py FreezeResult pattern |
| INV-T1b | IAM N action deny round-trip (SecurityArch 보완) | AC-1 | `scripts/verify_minio_iam_restore.py` | `s3:DeleteObject` → 403 + 소극적 deny | INV-S3 정합 |
| INV-T2 | forward path 6분 회복 | AC-2 | `docker logs mctrader-compactor \| grep "dual-write OK tier=L2"` | match ≥ 1 (6분 window) | RC-2 회복 증거 |
| INV-T2b | silent-skip log 0건 | AC-2 | `docker logs mctrader-compactor \| grep "_list_objects failed"` | match = 0 (post-AC-1) | post-Story-2 = 0 / 본 Story (Amendment 3 Proposed only) = grep 가드 정보 박제 |
| INV-T3 | ADR + DK 박제 | AC-3 | `git diff` Amendment 3 trail (line 41) + body (line 213) + History (line 651) + minio-bucket-policy-iam.md SSOT marker | diff 3 영역 모두 present | Phase 2 Group C cross-repo PR |
| INV-T4 | WS-A 16,946 files L2 승급 ratio ≥ 0.90 | AC-4 | `scripts/verify_ws_a_backfill_mct200.py --root /var/lib/mctrader/data --start 2026-05-13 --end 2026-05-15 --exchange upbit --channel orderbooksnapshot --threshold 0.90` | ratio ≥ 0.90 + audit/backfill-manifest YAML 무변 | MCT-173 D8=C pattern 정합 |
| INV-T5 | IAM drift detect cron (AC-5) | AC-5 | mc admin policy info + user list snapshot vs SSOT diff | diff = 0 (cron 일 1회) | OpRisk consult |
| INV-T6 | silent-skip grep 가드 | AC-3 | `rg -n "except.*ClientError.*:\s*\n.*log.*warning.*\n.*return None" src/mctrader_data/` | 본 Story = expected ≥ 1 (Amendment 3 Proposed 영역, 코드 변경 0 invariant 보존) / post-Story-2 LAND = 0 | ADR Amendment 3 §3.1 |
| INV-T7 | perf baseline within target | AC-4 | WS-A 백필 duration ≤ 24h + RSS ≤ 256 MB (MCT-163 F6 streaming) | duration ≤ 24h + RSS ≤ 256 MB | CLAUDE.md §Streaming refactor |

### §8.2 verify script outline

#### `scripts/verify_minio_iam_restore.py` (Phase 2 Group A)

- input: `--alias <mc-alias>` (또는 `--access-key/--secret-key`) + `--bucket mctrader-market`
- exit codes: 0 = 4 action allow + N action deny PASS / 1 = allow 1+ FAIL / 2 = deny PASS 위반 (over-grant)
- output JSON: `IamVerifyResult(put: bool, list: bool, head: bool, get: bool, delete_denied: bool)`
- `wal_freeze.py FreezeResult` dataclass pattern 정합 (operational script convention).
- **INV-RoundTrip (FIX Iter 2 carryover, ArchitectPL 판정 박제)**: DENY check 대상 = **script-owned object only** (`_iam_verify/` prefix, `generate_temp_key()` self-contained 패턴 재사용). 외부/실데이터 키(`l1/...` production) 금지 — broken-IAM failure path 에서 verify 가 실데이터 삭제 0 보장 + DENY signal deterministic (key 실존 무관). teardown = temp_key + sentinel 둘 다 403/404-tolerant. *향후 verify gate FIX 시 Orchestrator 지침이 외부 객체 가정 재발 차단 (FIX Iter 1 "외부 sentinel 무손상" 지침 결함 mitigation).*

#### `scripts/verify_ws_a_backfill_mct200.py` (Phase 2 Group B)

- MCT-173 `verify_backfill_partial_loss.py` pattern 재사용 — partition 별 WAL line count vs L1 row count 비율 검증.
- input: `--root <data-root> --start 2026-05-13 --end 2026-05-15 --exchange upbit --channel orderbooksnapshot --threshold 0.90`
- Phase 2.4 result 형식 박제 (`docs/audit/MCT-200-ws-a-backfill-verify-2026-05-13-15.md`):
  ```
  Total L1 rows: <N> / WAL frames: <M> (ratio ~Xx, orderbooksnapshot flatten)
  Pass=<P>, Fail=<F>, Skip=<S>
  INV-T4 PASS: True
  ```

### §8.3 성능 영향

본 Story = 코드 변경 0 → forward path 성능 영향 0. WS-A operator 수동 백필 (1회) 시 일시적 L2 buffer ~10GB + RSS ≤ 256 MB 검증 (MCT-163 F6 iter_batches streaming 의존, 이미 main 박제).

## §8.5 Stateful / Restart Invariant

§8.5_active = **true** (PL Phase 1.0 평가 확정 — IAM 복원 = MinIO container restart 후 policy 잔존 검증 영역).

### §8.5.1 long-running (24h sustained)

- MinIO 24h sustained policy + access-key 잔존 24/24 PASS.
- 검증: 1h cadence `mc admin policy info` hash diff = 0 + `mc admin user svcacct list <username>` 잔존 = 24/24.

### §8.5.2 restart recovery (**CRITICAL** — Researcher Unknown #1 회귀 차단)

```bash
# Pre-restart snapshot
mc admin policy info <alias> mctrader-market-admin --json > /tmp/policy-pre.json
docker exec mctrader-minio sha256sum /data/.minio.sys/config/iam/policies/*.json > /tmp/iam-hash-pre.txt

# Restart
docker restart mctrader-minio

# Post-restart verify (60s wait for ready)
sleep 60
mc admin policy info <alias> mctrader-market-admin --json > /tmp/policy-post.json
diff /tmp/policy-pre.json /tmp/policy-post.json  # diff = 0 의무
docker exec mctrader-minio sha256sum /data/.minio.sys/config/iam/policies/*.json > /tmp/iam-hash-post.txt
diff /tmp/iam-hash-pre.txt /tmp/iam-hash-post.txt  # hash 잔존 의무

# Volume mount 검증 (T-B3 CRITICAL 회귀 차단)
docker inspect mctrader-minio --format '{{json .Mounts}}' | jq '.[] | select(.Destination=="/data") | .Type'
# expected: "bind" or "volume" (NOT "tmpfs")
```

INV-T8.5.2 = green 조건: `diff = 0` (policy + IAM hash) + Mounts `/data` Type ∈ {"bind","volume"}.

### §8.5.3 idempotency replay (SIGKILL → restart 재실행)

- WS-A `promote-historical` 실행 → SIGKILL (중간) → 재실행.
- `.compacted` sentinel + NAS HEAD-then-PUT idempotency → 중복 처리 0건.
- Counter `mctrader_data_nas_uploader_idempotent_skip_total{tier,channel}` increment 검증 (ADR Amendment 3 §2 결정 4).
- INV-2 already_promoted no-op (CLAUDE.md §backfill mode).

### §8.5.4 volume mount 검증 (T-B3 CRITICAL 회귀 차단)

```bash
docker inspect mctrader-minio --format '{{range .Mounts}}{{.Source}} -> {{.Destination}} ({{.Type}}){{"\n"}}{{end}}'
# expected: 
#   /volume1/docker/minio/data -> /data (bind)   # 또는 named volume
```

INV-T8.5.4 = `/data` mount Type ≠ "tmpfs" + Source path 존재.

### §8.5 dissent 0 박제

PL Phase 1.0 평가 (§8.5_active=true) ↔ TestContractArchitectAgent SubAgent 분석 일치. dissent 0.

### §8.6 Phase 2 구현 산출물 (2026-05-18)

Phase 2 3-group parallel (코드 변경 0 — ops/scripts/runbook 영역). DeveloperPL roster: InfraEngineer (Group A) + DataEngineer (Group B) + ArchitectAgent (Group C cross-repo).

**Group A (InfraEngineerAgent) — IAM 복원 산출물**:
- `scripts/restore_minio_iam.sh` (11KB, exec) — idempotent (`mc admin policy info` diff → add/update), `--dry-run`(기본)/`--execute`/`--rollback --snapshot`, kill-switch ordering (access-key revoke → policy → user/group → 새 key → compactor 재기동 → verify), blue-green rotation (DataMigration §11.6), P2 SID↔SA mapping table (DesignReview P2 finding 처리)
- `scripts/verify_minio_iam_restore.py` (17KB, exec) — 4 action round-trip smoke (PUT/LIST/HEAD/GET sha256 byte-동형) + **N action deny** (s3:DeleteObject 403 expected, SecurityArch AC-1 보완) + Counter `mctrader_data_iam_verify_failures_total` emit (silent-skip 0) + exit 0/1 + `--output-json`
- `docs/runbooks/minio-bucket-policy-iam-restore.md` — 6-phase operator 절차 (RC-3 진단 게이트 Edge-RC2 / dry-run / 2-eyes approval / 실행 / AC-1·AC-2 verify / blue-green rotation)
- `docs/audit/MCT-200-minio-iam-pre-restore-snapshot.md` — 4 차원 (user/policy/policy-info/user-info) YAML template + RC-3 진단 3 check (persistence/env/live-403) + AC-1·AC-2 sign-off

**Group B (DataEngineerAgent) — WS-A 117GB 백필 산출물**:
- `docs/runbooks/ws-a-historical-promotion-operator.md` — 7-step (INV-E IAM 선결 gate / 디스크 ≥117GB×2 / promote-historical CLI verbatim / 3-terminal 모니터링 / `.compacted` sentinel 재실행 / verify / audit) + OpRisk `--max-concurrency` cap 권고
- `scripts/verify_ws_a_backfill_mct200.py` — MCT-173 `verify_backfill_partial_loss.py` pattern 재사용 (L1/L2 ratio ≥0.90, per-symbol breakdown, KRW-MATIC partial boundary Skip 허용, rglob discovery commit c169720 준수), audit md 자동 박제, exit 0/1/2
- `docs/audit/MCT-200-ws-a-backfill-verify-2026-05-13-15.md` — MCT-173 Phase 2.4 result template (10 invariant checklist INV-1~5 + INV-A~E + perf INV-T7)

**Group C (ArchitectAgent) — cross-repo carrier (mctrader-hub PR [#399](https://github.com/mclayer/mctrader-hub/pull/399))**:
- `mctrader-hub:docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md` — **MCT-200 amendment** 박제 3 영역 (Amendment trail line 42 / amendment body / History), +43 insert / 0 delete (732 lines, sibling byte-format 정합)
- `mctrader-hub:docs/domain-knowledge/domain/data-health/minio-bucket-policy-iam.md` — 신규 245줄 (§1-§9: IAM SSOT marker + 4 JSON policy + access-key lifecycle + idempotency INV-Idem-1~4 + 5 Tier trust boundary + STRIDE 13)
- mctrader-hub commit `5dab4c4`, branch `docs/adr-027-amend-nas-list-head-403`, ADR-020 Amendment 1 joint-phase narrow form (Refs mctrader-data#97)

**Group C stale fact 정정 (verified-via 의무 이행)**:
- Phase 1 CodebaseMapper line annotation (line 41/213/651, "Amendment 3" numbered naming) = **STALE**. mctrader-hub origin/main 이 PR #397 `INCIDENT-2026-05-17 amendment — NAS PUT 4xx fail-fast` (merged) 로 ADR-027 651→689 lines 변경.
- Group C working tree 직접 재확인 → ADR-027 convention 정합: `**MCT-NNN amendment 박제 (date)**` body-heading (numbered "Amendment N" heading 미사용). 신규 amendment = **"MCT-200 amendment"** (sibling MCT-160/MCT-164/INCIDENT-2026-05-17 동형). draft `RuntimeError` message 코드 literal "ADR-027 Amendment 3" 는 Story 2 구현 contract 로 verbatim 보존.
- **도메인 발견**: PR #397 INCIDENT-2026-05-17 amendment 가 NAS PUT *result* path silent-skip cover → MCT-200 amendment = NAS *read* path (LIST/HEAD) 영역 확장, disjoint cross-link. Cross-Story pattern N=3 = MCT-160(cadence) + MCT-164(source) + **MCT-200(NAS read path)**.

**verify gate 상태**: 산출물 ready (operator 수동 실행 대기). AC-1~AC-5 검증 = operator runbook 따라 Phase 2 LAND 후 실측 (audit md 자동 박제).

## §9 Review

### §9.1 설계 리뷰 (DesignReviewPLAgent, 2026-05-18)

**verdict_packet_v4.4**:
- `pl_recommendation`: **PASS**
- `cl_verdict`: PASS (Claude native review)
- `cx_verdict`: PASS (Phase 0.5 carry-over touchpoint_2 = §3 boundary ↔ ADR §결정 1~5 mapping 1:1 검증, deeper interface signature mechanical cross-check = Code review lane 책임 per ADR-068 §결정 2)
- `designreview_complete`: true
- `cross_repo_carrier_ready`: true
- `cross_story_pattern_continuity_validated`: true (Amendment 1 MCT-160 + Amendment 2 MCT-164 + Amendment 3 MCT-200 sibling 시리즈 형식 + invariant 영역 + Counter 패턴 + alert rule 모두 정합)
- `next_lane`: implementation
- `fix_counter`: 0/3

**4 invariants (ADR-068 §결정 2 dual-binding)**:
- I-1 (API contract semantic completeness): ADR Amendment 3 §결정 1-5 (Counter signature + RuntimeError signature + alert rule + idempotency Counter + Epic seed) interface contract 완결 ✓
- I-2 (cross-module propagation): Story §3.1 deputy 1 박제 (l2.py + runner.py + cli.py caller surface) + ADR §결정 5 fail-fast propagation Story 2 위탁 명시 ✓
- I-3 (unconditional vs conditional guard placement intent): §11.6 blue-green = unconditional (rotation 시) / §7.4.5 clock drift = conditional (drift > 1s) intent clear ✓
- I-4 (wording SSOT): "silent-skip", "fail-fast", "blue-green", "NAS-side LIST/HEAD" 일관 (Story + ADR draft + DK 3 file cross-check) ✓

**findings 3건 (P0 차단 0)**:
- **P2** (Phase 2 Group A 책임 위탁): §11.1 + DK §2 — bucket policy JSON Sid (PascalCamelCase) ↔ DK Service Account (kebab-case) naming drift. Phase 2 `restore_minio_iam.sh` 작성 시 SA-name ↔ policy-name mapping table 의무 (drift detection 명시).
- **P3** (정보성): §3.4 + §6 cross-repo carrier transfer obligation info-only — Phase 2 Orchestrator 가 cross-repo PR open 시 `.architect-output/` 2 file → mctrader-hub:docs/adr + docs/domain-knowledge 으로 transfer.
- **P3** (정보성): §8.1 INV-T2b vs INV-T6 wording 명료성 — INV-T2b "match = 0 (post-AC-1)" 와 INV-T6 (grep 가드 expected ≥ 1) 별개 측면 정합 OK.

**Phase 1 PR open prerequisite**: **10/10 green / 0/10 red**.

**lane PL self-write 0 — 본 §9.1 = Orchestrator post-Sonnet 영역 append (ADR-022 Deprecated, Sonnet decider 자동 발동 무효 — Orchestrator 직접 write)**.

## §10 FIX Ledger

> Orchestrator 단독 관리 (fix-event-v1 contract, CFP-32 monopoly). 카운터 SSOT.

| Iter | 시각 | 레인 | 트리거 | 원인 판정 | 재실행 범위 | RESET? |
|------|------|------|--------|-----------|-------------|--------|
| 1 | 2026-05-18T00:4x:00Z | 구현-리뷰 (pre / DeveloperPL verdict) | DeveloperPL P1×1 + P2×2 + P3×1 (restore_minio_iam.sh readonly 재할당 / verify_minio_iam_restore.py teardown-after-deny / verify_ws_a_backfill_mct200.py audit_path root.parent / DK §2 SA name drift) | **구현** (root-cause-decision: 모두 local, 설계 escalate 조건 미충족, Change Plan §13.2/§8.2 의도 정상 명시) | Phase 2 PR commit append — Group A InfraEngineer (P1+P2) / Group B DataEngineer (P2) / Group C ArchitectAgent (P3 DK SSOT 통일) | — (구현-리뷰 1/3, 동일 lane append) |
| 2 | 2026-05-18T01:xx:00Z | 구현-리뷰 (CodeReviewPL 2-layer) | CodeReviewPL P1×1 + P2×1 + P3×1 (verify_minio_iam_restore.py sentinel_key script-owned 아님 = **FIX Iter 1 P2 해소 side-effect** / verify_ws_a_backfill_mct200.py L2-orphan guard 우회 false-PASS / restore_minio_iam.sh tee flush deferrable) | **구현** (root-cause-decision: 모두 local, 설계 escalate 미충족 — §8 round-trip self-contained 원칙 존재, 설계 의도 DENY≠teardown 분리 불변, 구현 selection 만 부정확) | Phase 2 PR commit append — Group A (P1 script-owned sentinel) / Group B (P2 L2-orphan branch) / P3 ArchitectPL defer 판단. CodeReview FIX 경로 = DeveloperPL 1차 진단 → ArchitectPL 최종 판정 | — (구현-리뷰 2/3, 동일 lane append) |
| 3 | 2026-05-18T02:xx:00Z | 구현-리뷰 (DeveloperPL Iter 2 재검수) | DeveloperPL 신규 P2×2 (verify_minio_iam_restore.py:106 `all_pass` 주석 `pass_count==4` stale → 실제 5 / :582 summary log `Pass Count: {n}/4 (PUT,LIST,HEAD,GET)` stale — FIX Iter 2 sentinel PUT action 추가로 pass_count 4→5 변경 시 주석/로그 동기화 누락). P1/P2 **원본 resolved** | **구현 (mechanical stale string)** — logic 변경 0, FIX Iter 2 commit 298a03a 의 pass_count 4→5 변경 시 (a) dataclass 주석 (b) summary log string 동시 갱신 누락. Orchestrator FIX Iter 2 spawn 지침이 all_pass 변경만 명시, 주석/로그 동기화 미명시한 결함 일부 포함 | verify_minio_iam_restore.py:106 주석 + :582 log string 2행 (mechanical fast-path, DeveloperAgent 단독) | — (**구현-리뷰 3/3 도달** — RESET 불요, escalation trigger 3종 미충족) |

**FIX Iter 3 / 3-of-3 reassessment (ADR-067 §결정 2, DeveloperPL 검토)**:
- **reassessment_3of3_triggered: true** (구현-리뷰 Iter 1 + Iter 2 + Iter 3 = 3/3). ArchitectPL implementability reassessment 형식 의무.
- **escalation trigger 3종 (DeveloperPL 판정 — ArchitectPL confirm 의무)**:
  - (i) design granularity inadequate → **미충족** (구현 체크리스트 미완 — pass_count 4→5 변경 시 주석/로그 미동기화, 설계 granularity 부족 아님)
  - (ii) cross-module invariant 위반 without convergence → **미충족** (단일 파일 verify_minio_iam_restore.py 내 stale string)
  - (iii) DeveloperPL↔ArchitectPL N+1 divergence → **미충족** (Iter1·2·3 모두 구현 원인 일관, 설계 재해석 불일치 0)
- **escalation 결론**: 3종 모두 미충족 → 사용자 escalation 불발동 (ADR-067 §결정 2, 0 충족 시 RESET path 선택 가능). ArchitectPL reassessment 가 confirm 후 Iter 3 mechanical fast-path 진행.
- **Iter 3 scope (mechanical fast-path — logic 0)**: `verify_minio_iam_restore.py:106` 주석 `pass_count == 4` → `pass_count == 5`, `:582` log `{n}/4 (PUT, LIST, HEAD, GET)` → `{n}/5 (SENTINEL_PUT, PUT, LIST, HEAD, GET)`. DeveloperAgent 단독 or Orchestrator 직접.

**FIX Iter 3 reasoning_carryover (fix-event-v1 v1.2, ArchitectPL reassessment 입력)**:
- `invariant_summary`: 코드 변경 0 (src/** 0); P1/P2 원본 resolved; Iter 3 = stale string 2행 logic 변경 0
- `disputed_claims`: 없음 (DevPL 판정 = mechanical stale string, escalation trigger 3종 미충족, divergence 0). ArchitectPL reassessment = 형식 confirm
- `transcript_ref`: §10 Iter 3 row + DeveloperPL Iter 2 재검수 verdict (P1/P2 resolved, new P2×2 stale string)

**FIX Iter 3 / 3-of-3 ArchitectPL implementability reassessment (2026-05-18)**:
- trigger (i) design granularity / (ii) cross-module invariant / (iii) DevPL↔ArchPL N+1 divergence = **3종 모두 미충족** (DeveloperPL 판정 ArchitectPL 독립 confirm — Iter1·2·3 PL 판정 구현 원인 일관 수렴, divergence 0).
- **escalation_decision: 불발동** (0/3 trigger, ADR-067 §결정 2 의무 escalation 미해당, RESET path 선택 — 사용자 escalation 생략).
- `iter3_mechanical_fastpath_eligible: true` (logic-zero documentation string 2행, grep-confirmable). `iter3_review_scope: DeveloperPL 재검수만` (CodeReview 2-layer 재spawn 불요, 보안/정합 표면적 0).
- `implementability_verdict: OK (수렴 명확)` — P1/P2 원본 resolved, Iter 3 = stale string, 설계 결함 부재. 추가 발견: `verify_minio_iam_restore.py:337` docstring 은 이미 `pass_count==5` 정합 (asymmetric staleness — F1/F2 = 고립된 2-line oversight, systemic 아님).
- **side-effect 연쇄 mitigation (ArchitectPL 권고)**: Iter1(P1 readonly)→Iter2(P1 sentinel = Iter1 P2 side-effect)→Iter3(F1/F2 = Iter2 pass_count 4→5 side-effect) 3-hop 연쇄. 근본 = FIX spawn 지침이 변경 site 만 명시·동기 산출물(주석/log/계약 mirror) 미명시. **carryover 룰: 향후 FIX spawn 지침에 "값 변경 시 연관 주석/log/계약 mirror 동시 갱신 체크리스트" 명시 의무** (재발 차단). §8.2 INV-RoundTrip 박제 완료.

**FIX Iter 3 CLOSE (mechanical fast-path, 2026-05-18)**: Orchestrator 직접 mechanical fix — `verify_minio_iam_restore.py:106` 주석 `pass_count == 4` → `== 5 (FIX Iter 2: +SENTINEL_PUT action 0)`, `:582` log `{n}/4 (PUT, LIST, HEAD, GET)` → `{n}/5 (SENTINEL_PUT, PUT, LIST, HEAD, GET)`. logic 변경 0. `python -m py_compile` PASS. DeveloperPL 재검수 (단독, CodeReview 2-layer 불요) 대기 → PASS 시 구현-리뷰 lane green (3/3 도달 but escalation 불발동, 수렴).

**reasoning_carryover (fix-event-v1 v1.2)**:
- `invariant_summary`: 코드 변경 0 (src/** 0) + bash readonly 미사용 (wal_freeze.py 정합)
- `disputed_claims`: 없음 (DevPL 1차 진단 ↔ root-cause-decision 판정 일치, 구현 원인 확정, ArchitectPL 회부 불요)
- `transcript_ref`: §8.6 stale 정정 (normal_self_correct, fix_ledger_append 불요 — Group C PASS)

**FIX Iter 1 액션 (구현 원인 → Change Plan 유지, Phase 2 PR commit append)**:
- P1: `restore_minio_iam.sh` `readonly MINIO_ALIAS`/`POLICY_DIR` 선언 제거 (parse_args 재할당 호환, wal_freeze.py readonly 미사용 패턴 정합)
- P2: `verify_minio_iam_restore.py` sentinel_key (`l1/_verify_sentinel_read_only`) 분리 — DELETE DENY check ↔ temp_key teardown 격리 (DENY 정상 시 temp object 잔존 모순 해소)
- P2: `verify_ws_a_backfill_mct200.py` audit_path = `Path(__file__).resolve().parents[1]/docs/audit/` (repo-relative, `root.parent` production filesystem 회피) + `--audit-output` 명시 인자
- P3: SA name SSOT 통일 = `mctrader-ingester` (write.json Sid `MctraderMarketIngestionOnlyWrite` 의미 정합) — restore script + runbook (Group A) align. Group C 검수: DK §2 = 이미 SSOT 정답, mctrader-hub#399 unchanged

**FIX Iter 1 CLOSE (DeveloperPL 재검수, 2026-05-18, commit 6a90a6c)**: pl_recommendation=**PASS**. P1/P2(teardown)/P2(audit_path)/P3 ALL resolved (verified-via file:line 직접 Read). new_findings=0. `fix_ledger_iter1_close: true`. 구현-리뷰 lane prerequisite **green**. 부수 정정: Group B FIX cwd 오류 (메인 repo main branch 에 생성) → Orchestrator 가 정답 버전 worktree 이전 (6a90a6c). 메인 repo untracked 잔존 = 사용자 작업 공간 별도 정리 대상 (FIX Ledger 범위 외). next_lane: code-review.

**FIX Iter 2 reasoning_carryover (fix-event-v1 v1.2, ArchitectPL re-spawn 입력 의무)**:
- `invariant_summary`: 코드 변경 0 (src/** 0), DENY≠teardown 분리 설계 의도 불변, §8 verify gate round-trip self-contained 원칙
- `disputed_claims`: P1 = FIX Iter 1 P2 해소 side-effect — Orchestrator FIX Iter 1 지침 결함 포함 ("sentinel 실제 L1 객체면 403 무손상" 가정 오류: broken-IAM 시 sentinel 실존하면 verify 가 실데이터 삭제). fix 방향 후보 = (a) script-owned sentinel `_iam_verify/` PUT 후 DENY (b) temp_key DENY + 403-tolerant teardown. ArchitectPL 최종 판정 영역
- `transcript_ref`: §10 Iter 2 row + CodeReviewPL verdict (cl=FIX, cx=UNAVAILABLE)

**FIX Iter 2 액션 (구현 원인 → Change Plan 유지, Phase 2 PR commit append)**:
- P1: `verify_minio_iam_restore.py` sentinel_key script-owned 전환 (`_iam_verify/` prefix 자체 PUT 후 DENY, OR temp_key DENY + 403-tolerant teardown) — broken-IAM failure path 에서 실데이터 삭제 0 + DENY 검증 deterministic. ArchitectPL 최종 fix 방향 판정 의무
- P2: `verify_ws_a_backfill_mct200.py:234-238` 명시 `elif l1_count==0 and l2_count>0:` → FAIL/anomaly 분기 (L2-orphan false-PASS 차단) + audit md 분류 정합
- P3: `restore_minio_iam.sh:83-84` tee flush — ArchitectPL defer/수용 판단 (non-blocking, exit-code masking 아님)

**FIX Iter 2 ArchitectPL 최종 판정 (DeveloperPL 1차 진단 → ArchitectPL, 2026-05-18)**:
- **P1 cause_final = 구현** (`p1_escalate: false`) — §8.2 self-contained round-trip + §8.1 INV-T1b deny round-trip 귀속 = 설계 granularity 충분, DENY 대상=script-owned 이 설계 정합. 구현 selection 만 이탈. Change Plan/§8 unchanged → Phase 2 PR commit append (설계리뷰 재실행 불요).
- **P1 fix_direction_final = (a) script-owned sentinel** — DeveloperPL 반론 **의무 수용** (사용자 directive 2026-05-13). (b) temp_key 403-tolerant = DENY-success=IAM-blocks=teardown-blocked 모순 해소 불완전 + temp_key 잔류 → 기각. `generate_temp_key()` 기존 self-contained 패턴 재사용.
- **P2 = dup-local 확정** (P1 과 file path disjoint — verify_minio_iam_restore.py ↔ verify_ws_a_backfill_mct200.py, 병행 안전).
- **P3 = defer** (non-blocking, exit-code masking 無, Story 2/후속 hardening 위탁).
- **Orchestrator FIX Iter 1 지침 결함 mitigation**: §8.2 INV-RoundTrip 박제 완료 (DENY 대상=script-owned, 외부/실데이터 키 금지 — 재발 차단).
- **3/3 risk = LOW** (기존 패턴 재사용 + file disjoint, 수렴 가능성 높음). 단 재검수 FAIL 시 구현-리뷰 3/3 도달 → ADR-067 §결정 2 escalation trigger 3종 즉시 검토 의무.

**reasoning_carryover_next (Iter 2 → 재검수 입력, fix-event-v1 v1.2)**:
- `invariant_summary`: 코드 변경 0 (src/** 0); DENY 대상=script-owned object; round-trip self-contained; teardown 403/404-tolerant
- `disputed_claims`: P1 구현 확정 (설계 escalate 미충족, §8.2+INV-T1b 충분 granular). Orchestrator FIX Iter1 "외부 sentinel 무손상" 지침 결함 §8.2 carryover 박제 완료. (a) 의무 수용, (b) 기각.
- `transcript_ref`: §10 Iter 2 row + §8.2 INV-RoundTrip + verify_minio_iam_restore.py:330,397

**FIX Iter 2 재구현 지침 (ArchitectPL → Group A/B 병렬, file disjoint)**:
- **Group A P1** (`verify_minio_iam_restore.py`): (1) `generate_temp_key()` 패턴 재사용 — sentinel = `_iam_verify/<ts>-<uuid>-sentinel.bin` script-owned (2) run_verify setup 에서 sentinel **PUT** → `action_delete_deny(client, bucket, sentinel_key)` (3) teardown: temp_key + sentinel_key 둘 다 삭제 시도, 403/404-tolerant (deny 작동 시 sentinel 403 정상=warning 억제, 부재 404 무시; deny 미작동 시 sentinel=script-owned 1KB 더미 → 삭제돼도 production 무손상) (4) line 397 하드코딩 `"l1/_verify_sentinel_read_only"` 제거 + DESIGN NOTE script-owned 변형 갱신 (5) `IamVerifyResult.delete_denied` 의미 + exit 0/1/2 불변
- **Group B P2** (`verify_ws_a_backfill_mct200.py:234-238`): KRW-MATIC guard 뒤 `elif l1_count==0 and l2_count>0:` → `fail_count += 1` + anomaly log + audit md (~line 312) 동일 패턴 동시 수정

## §11 데이터 마이그레이션 / Rollback

DataMigrationArchitectAgent primary self-write + OperationalRiskArchitectAgent §11.6 idempotency consult 통합.

### §11.1 bucket policy idempotent restore

mc admin policy add re-entry safe + diff 비교 후 변경 시에만 update + 부분 복원 회피 atomic transaction (Edge-RC1).

```bash
restore_policy() {
  local policy_name=$1
  local policy_file=$2

  if mc admin policy info <alias> "$policy_name" >/dev/null 2>&1; then
    # 기존 정책 존재 — diff 비교 후 변경 시에만 update
    mc admin policy info <alias> "$policy_name" --json | jq '.policy' > /tmp/current.json
    if ! diff -q /tmp/current.json "$policy_file" >/dev/null; then
      mc admin policy update <alias> "$policy_name" "$policy_file"
    fi
  else
    mc admin policy add <alias> "$policy_name" "$policy_file"
  fi
}

# 4 policy 박제 후 atomic attach (실패 시 pre-restore snapshot rollback)
for policy in read write list admin; do
  restore_policy "mctrader-market-${policy}" "scripts/minio-policies/${policy}.json"
done
mc admin policy attach <alias> mctrader-market-admin --user mctrader-admin
```

### §11.2 WS-A 117GB 백필 invariant (9건 통합 — CLAUDE.md §backfill mode 5 + §historical tier promotion 4)

| INV | 영역 | 의미 |
|-----|------|------|
| INV-1 | Source WAL immutable (PIT, D3=A) | PIT snapshot — WAL 변형 금지 |
| INV-2 | `.compacted` sentinel skip (D4=A, ADR-017 §D2) | already_promoted no-op, 재실행 safe |
| INV-3 | `_ob_snapshot_dicts_to_arrow()` 재사용 | MCT-166 path B schema 정합 |
| INV-4 | BackfillManifest YAML | `<root>/audit/backfill-manifest-<exchange>-<channel>.yaml` |
| INV-5 | MCT-165 V2=0 + partial loss within threshold (D8=C) | 양쪽 통과 후 retro |
| INV-A | forward `_run_l2`/`_run_l3` 윈도우 불변 | regression 차단 |
| INV-B | 무손실 — dual_writer.write committed + WS-B `scan_and_cleanup_legacy` 4중 HEAD verify | promote_l1 4중 HEAD verify gate |
| INV-C | deterministic run_id + sha256 idempotency | NAS PUT HEAD-then-PUT |
| INV-D | channel 한정 (orderbooksnapshot only) | orderbookdepth = #48 별 Story |
| **INV-E (본 Story 추가)** | **IAM 선결 prerequisite** | Group A LAND 후 Group B 진입 (RC-1 회복 의무) |

### §11.3 Rollback 3 sub

#### §11.3.1 bucket policy 부분 복원 rollback (Edge-RC1)

```bash
# pre-restore snapshot 의무 (docs/audit/MCT-200-minio-iam-pre-restore-snapshot.md)
restore_minio_iam.sh --rollback --snapshot docs/audit/MCT-200-minio-iam-pre-restore-snapshot.yaml
# 부분 복원 atomic rollback — 4 policy 모두 박제 후 단일 attach transaction
```

#### §11.3.2 WS-A 백필 부분 실패 rollback (Edge-RC3)

- L2 부분 출력 삭제 (run_id deterministic — 동일 run_id 출력 file 제거)
- `.compacted` sentinel 해제 (없으면 no-op)
- 재실행 safe (INV-2 already_promoted + INV-C HEAD-then-PUT sha256 idempotency)

#### §11.3.3 ADR-027 Amendment 3 Proposed → Story 2 진입 rollback

- ADR Amendment 3 status downgrade (Proposed → Draft) 또는 git revert
- mctrader-hub PR revert 시 ADR-045 §결정 3 의 forcing function 보존 (Story 2 진입 가능 조건 = Amendment 3 Accepted, 미Accepted 시 Story 2 spawn 차단)

### §11.4 Data integrity 6 invariant

1. bucket policy version-controlled JSON (`scripts/minio-policies/*.json` git tree SSOT)
2. access-key NEVER git-tracked (`.env` runtime injection only, INV-S1)
3. rotation audit log append-only (`docs/audit/MCT-200-iam-rotation-log.md`, 1년 보존)
4. WS-A partition boundary date + symbol (deterministic run_id)
5. WS-A L2 NAS PUT sha256 idempotency (HEAD-then-PUT Metadata['sha256'])
6. ADR-027 Amendment 3 fail-fast unsupported combo (RuntimeError raise + Counter emit, post-Story-2 LAND)

### §11.5 schema evolution

본 Story 코드 변경 0 → schema evolution **0건**.

### §11.6 Idempotency consult (OpRisk primary)

- `mc admin policy create/add` overwrite (idempotent) — §11.1 정합.
- `mc admin policy attach` 재실행 = no-op (이미 attach 된 경우 silent OK).
- access-key rotation = **blue-green 패턴** 의무 (direct update 금지 — in-flight PUT fail). minio-bucket-policy-iam.md §3.3 verbatim:
  1. BLUE 새 svcacct 추가 (기존 잔존)
  2. verify 신규 키로 4 action round-trip smoke
  3. compactor / collector `.env` rotate + graceful restart
  4. 6분 모니터링
  5. GREEN old svcacct 제거
- WS-A 백필 `.compacted` sentinel + already_promoted no-op + HEAD-then-PUT sha256 idempotency.
- 부분 IAM 복원 atomic rollback (§11.3.1 Edge-RC1).

### §11.7 적극 이의 3건 cross-ref

- **HIGH (manifest write ↔ sentinel write atomicity)**: 현재 write 순서 별 transaction → crash window 가능. → **Story 2 별 Epic CodebaseMapper 위탁** (본 Story 코드 변경 0 invariant 보존).
- **MEDIUM (access-key rotation Point of no return)**: rotation 시점 = WS-A idle + forward idle 동시 만족 + atomic transaction explicit confirm gate. → §13 runbook 강제.
- **LOW (bucket policy JSON SSOT git tree 갱신 누락)**: operator workflow "git diff PASS 후 `--execute`" runbook 강제. → §13.1 2-eyes approval gate + §11.4 #1 invariant.

## §13 Live Operational Discipline (LiveOpsDeputyAgent CONDITIONAL ACTIVE)

LiveOpsDeputyAgent self-write — production MinIO bucket IAM credential 변경 영역 (live_touching=true, deputy_spawn=7).

### §13.1 Operator approval workflow (2-eyes 강제)

- 1 executor + 1 reviewer ≠ executor
- Issue #97 comment channel "approved-by: @<reviewer>" 의무
- 단일 operator 환경 = 30분 cooling-off self-review (alternative gate)
- 적용 영역: IAM 복원 / access-key rotation / mc admin policy update / bucket policy JSON git tree edit

### §13.2 Kill switch (IAM 복원 emergency rollback)

```bash
# IAM 복원 script standard
set -euo pipefail

# 단계 ordering (실패 시 즉시 abort)
# 1. access-key revoke first (in-flight PUT 차단)
mc admin user svcacct rm <alias> <old-access-key>
# 2. policy 박제
for p in read write list admin; do
  restore_policy "mctrader-market-${p}" "scripts/minio-policies/${p}.json"
done
# 3. user / group restore
mc admin user add <alias> <username> <secret>
# 4. 새 access-key 생성
mc admin user svcacct add <alias> <username> --access-key <NEW> --secret-key <NEW>
# 5. compactor 재기동
docker compose restart mctrader-compactor
# 6. verify
python scripts/verify_minio_iam_restore.py

# WS-A graceful shutdown 시 emergency rollback
# mc admin policy detach + snapshot restore (docs/audit/MCT-200-minio-iam-pre-restore-snapshot.yaml)
```

### §13.3 2 Runbook outline

#### `docs/runbooks/minio-bucket-policy-iam-restore.md` (Phase 1 PR scope, Phase 2 Group A 적용)

1. 사전 박제 — pre-restore snapshot (mc admin policy/user/group/svcacct list)
2. dry-run — `scripts/restore_minio_iam.sh --dry-run`
3. 2-eyes approval (§13.1) — Issue #97 comment "approved-by: @<reviewer>"
4. 실 실행 — `scripts/restore_minio_iam.sh --execute`
5. verify — `scripts/verify_minio_iam_restore.py` (4 action allow + N action deny)
6. 6분 모니터링 — `docker logs mctrader-compactor | grep "dual-write OK tier=L2"`
7. 비상 절차 (§13.2 kill switch)

#### `docs/runbooks/ws-a-historical-promotion-operator.md` (Phase 2 Group B scope)

1. 사전 조건 — INV-E IAM prerequisite (`verify_minio_iam_restore.py` PASS)
2. `promote-historical` 실행 (CLAUDE.md §historical tier promotion 코드 블록 verbatim)
3. 진행 모니터링 — Counter `mctrader_data_nas_uploader_idempotent_skip_total` + log grep
4. 부분 실패 처리 (§11.3.2)
5. verify — `scripts/verify_ws_a_backfill_mct200.py --threshold 0.90`
6. 종료 박제 — `docs/audit/MCT-200-ws-a-backfill-verify-2026-05-13-15.md`
7. 비상 절차 (§13.2 kill switch — WS-A graceful shutdown)

### §13.4 IAM credential lifecycle

- revoke + recreate (update 회피 — blue-green §11.6)
- rotation 90일 cadence (`.env` 0600 + OpRisk §7.4.4)
- vault migration 별 Epic (현재 runtime env injection KEEP)
- audit log SSOT: `docs/audit/MCT-200-iam-rotation-log.md` (append-only, 1년 보존, T-A3 완화)

### §13.5 Incident response (Issue #97 SSOT)

- Issue #97 = incident timeline SSOT (T0-T6)
- post-mortem 의무: `docs/post-mortem/MCT-200-minio-iam-403-incident.md`
  - §3 timeline + §4 RCA 5-why + §5 회귀 방지 + §6 cross-Story pattern N=3 박제 (ADR-045 Amendment 5 §D-9 정합)

### §13.6 Production rollout 가시성

| 채널 | 의무 수준 | 영역 |
|------|----------|------|
| Issue #97 comment | MUST | 모든 step (dry-run/approval/execute/verify/모니터링/종료) |
| `docker logs` 30분 모니터링 | MUST | post-execute (dual-write OK tier=L2 + silent-skip 0) |
| WS-B sweep 52h 추세 weekly | SHOULD | WS-A LAND 후 weekly 추세 박제 |
| Slack/Discord | MAY | 운영 환경 정보 부족 → 본 Story OUT |

### §13.7 Live Discipline 12 행 표 (§13.1-§13.6 + §7/§11 cross-ref)

| # | item | severity | source | cross-ref |
|---|------|----------|--------|-----------|
| 1 | 2-eyes approval | MUST | §13.1 | §7.3 T-A2 / §11.7 LOW |
| 2 | IAM 복원 ordering (revoke → policy → user → key → restart → verify) | MUST | §13.2 | §11.6 blue-green |
| 3 | pre-restore snapshot 박제 | MUST | §13.2 / §13.3 step 1 | §11.3.1 rollback |
| 4 | mc admin policy info 직접 검증 (Edge-RC2) | MUST | §13.3 step 1 + Phase 2 1st step | §7.4.1 DR gate |
| 5 | dry-run 의무 | MUST | §13.3 step 2 | §11.1 idempotent |
| 6 | Issue #97 comment audit trail | MUST | §13.5 | §7.3 T-A3 |
| 7 | 4 action allow + N action deny verify | MUST | §13.3 step 5 | §8.1 INV-T1+T1b |
| 8 | 6분 monitoring (dual-write OK) | MUST | §13.3 step 6 + §13.6 | §8.1 INV-T2 |
| 9 | WS-A `--max-concurrency=2` cap | MUST | §13.3 (B) step 2 | §7.4.3 R5 |
| 10 | post-mortem 의무 | MUST | §13.5 | ADR-045 §D-9 |
| 11 | access-key blue-green rotation | MUST | §13.4 | §11.6 R8 |
| 12 | docker logs 30분 모니터링 | MUST | §13.6 | §8.5.2 restart recovery |

PMO self-write field (Phase 2 LAND 시점 결과 박제 — ADR-045 Amendment 5 §D-9 mandatory framing):

```yaml
pmo_output_v1.2:
  cross_story_pattern_adr_trigger:
    threshold_check: REACHED (N=3)
    pattern_class: "silent-skip fail-fast invariant 영역 확장 (ADR-027 sibling 시리즈)"
    evidence:
      - story: MCT-160 (cadence trigger silent-skip) — ADR-027 Amendment 1
      - story: MCT-164 (multi-channel source silent-skip) — ADR-027 Amendment 2
      - story: MCT-200 (본 Story — NAS-side LIST/HEAD silent-skip) — ADR-027 Amendment 3 (Proposed)
    detection_channel: "primary anchor_id strict matching + secondary root_cause_class fallback"
    escalation_action: adr_draft_emitted (default)
    forcing_function: "사용자 결정 1 (본 Story 내 ADR draft 포함) 정합. Story 2 (별 Epic 위탁 — silent-skip 코드 fix) = downstream consumer (ADR Accepted 후 진입)."
```
