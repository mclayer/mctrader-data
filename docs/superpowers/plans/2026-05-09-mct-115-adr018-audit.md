# MCT-115: ADR-018 7-Pattern Audit — mctrader-data Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ADR-018 7패턴(D1~D7) 위반 0건 달성 — WAL/compactor 신규 코드 포함 전체 mctrader-data 스캔 후 fix.

**Architecture:** 발견된 위반 6건(D1×4, D2×1, D3×1) + 주석 개선 1건(D5)을 TDD로 fix. D4/D6/D7은 N/A + 사유 명시. 모든 변경은 기존 280 tests GREEN 유지 후 PR merge.

**Tech Stack:** Python 3.12, Pydantic v2, `@dataclass`, pytest 9.0

---

## 감사 결과 요약

| 패턴 | 파일 | 위반 내용 | 조치 |
|------|------|-----------|------|
| D1 | `tick_storage.py:TickRecord` | `price`/`quantity` float 미거부 | Task 1 |
| D1 | `orderbook_storage.py:OrderbookEventRecord` | `price`/`quantity` float 미거부 | Task 1 |
| D1 | `orderbook_snapshot_storage.py:OrderbookSnapshotRecord` | `price`/`quantity` float 미거부 | Task 1 |
| D1 | `metadata_storage.py:ExchangeMetadataRecord` | Decimal 필드 float 미거부 | Task 1 |
| D2 | `manifest.py:CollectorManifest` | `frozen=True` 누락; `list[str]` → tuple 미전환 | Task 2 |
| D3 | `schema.py:OhlcvRow` | `low <= high` cross-field validator 누락 | Task 3 |
| D5-주석 | `wal/ingester.py:WalIngester` | O_APPEND vs tmp-rename 패턴 사유 미명시 | Task 4 |
| D4 | — | asyncio CompactorRunner 단일 스레드; WalIngester threading.Lock ✓ | N/A |
| D6 | — | health_server GET-only, 보안 헤더 파싱 없음; httpx Headers case-insensitive ✓ | N/A |
| D7 | — | CLI에 --skip-validation 류 bypass flag 없음 ✓ | N/A |

**PASS 항목:**
- `OhlcvRow` (Pydantic strict + `Decimal38_18` BeforeValidator: float → ValueError) ✓
- `ndjson_codec.decode_line()` (`parse_float=Decimal`) ✓
- `l1._tick_dicts_to_arrow()` (`Decimal(str(...))` 패턴) ✓
- 모든 파일 writer: uuid/.tmp 기반 atomic rename ✓
- `compactor/l1.py`: `tempfile.mkstemp()` → `os.replace()` ✓
- `compactor/l2.py`, `l3.py`: PID .tmp → `os.replace()` ✓
- `compactor/gc.py`: 마커 기반 삭제, 크래시 안전 ✓

---

## 파일 변경 목록

| 파일 | 변경 유형 | 내용 |
|------|---------|------|
| `src/mctrader_data/tick_storage.py` | Modify | `TickRecord.__post_init__` 추가 |
| `src/mctrader_data/orderbook_storage.py` | Modify | `OrderbookEventRecord.__post_init__` 추가 |
| `src/mctrader_data/orderbook_snapshot_storage.py` | Modify | `OrderbookSnapshotRecord.__post_init__` 추가 |
| `src/mctrader_data/metadata_storage.py` | Modify | `ExchangeMetadataRecord.__post_init__` 추가 |
| `src/mctrader_data/manifest.py` | Modify | `CollectorManifest` frozen+tuple |
| `src/mctrader_data/schema.py` | Modify | `OhlcvRow` model_validator 추가 |
| `src/mctrader_data/wal/ingester.py` | Modify | O_APPEND 패턴 주석 추가 |
| `tests/test_adr018_d1_d3.py` | Create | D1/D2/D3 위반 검증 tests |

---

## Task 1: D1 — dataclass Decimal float 거부 (4개 dataclass)

**Files:**
- Modify: `src/mctrader_data/tick_storage.py:51-59` (`TickRecord`)
- Modify: `src/mctrader_data/orderbook_storage.py:57-68` (`OrderbookEventRecord`)
- Modify: `src/mctrader_data/orderbook_snapshot_storage.py` (`OrderbookSnapshotRecord`)
- Modify: `src/mctrader_data/metadata_storage.py` (`ExchangeMetadataRecord`)
- Create: `tests/test_adr018_d1_d3.py`

- [ ] **Step 1: Write failing tests**

파일 `tests/test_adr018_d1_d3.py` 생성:

```python
"""ADR-018 D1/D2/D3 패턴 위반 감사 테스트 (MCT-115)."""
from __future__ import annotations

from datetime import date, datetime, timezone
from decimal import Decimal

import pytest

from mctrader_data.tick_storage import TickRecord
from mctrader_data.orderbook_storage import OrderbookEventRecord
from mctrader_data.orderbook_snapshot_storage import OrderbookSnapshotRecord
from mctrader_data.metadata_storage import ExchangeMetadataRecord


_TS = datetime(2026, 5, 1, 0, 0, tzinfo=timezone.utc)


# ─── D1: TickRecord float 거부 ────────────────────────────────────────────────

def test_tick_record_rejects_float_price() -> None:
    with pytest.raises(TypeError, match="float"):
        TickRecord(
            ts_utc=_TS, received_at=_TS,
            exchange="bithumb", symbol="KRW-BTC",
            price=1.5,  # float — should raise
            quantity=Decimal("0.001"), side="buy",
        )


def test_tick_record_rejects_float_quantity() -> None:
    with pytest.raises(TypeError, match="float"):
        TickRecord(
            ts_utc=_TS, received_at=_TS,
            exchange="bithumb", symbol="KRW-BTC",
            price=Decimal("100000000"), quantity=0.001,  # float — should raise
            side="buy",
        )


def test_tick_record_accepts_decimal() -> None:
    r = TickRecord(
        ts_utc=_TS, received_at=_TS,
        exchange="bithumb", symbol="KRW-BTC",
        price=Decimal("100000000"), quantity=Decimal("0.001"), side="buy",
    )
    assert r.price == Decimal("100000000")


# ─── D1: OrderbookEventRecord float 거부 ─────────────────────────────────────

def test_orderbook_event_record_rejects_float_price() -> None:
    with pytest.raises(TypeError, match="float"):
        OrderbookEventRecord(
            ts_utc=_TS, received_at=_TS,
            exchange="bithumb", symbol="KRW-BTC",
            event_type="snapshot", side="bid", level=0,
            price=100.5,  # float — should raise
            quantity=Decimal("1.0"),
        )


def test_orderbook_event_record_rejects_float_quantity() -> None:
    with pytest.raises(TypeError, match="float"):
        OrderbookEventRecord(
            ts_utc=_TS, received_at=_TS,
            exchange="bithumb", symbol="KRW-BTC",
            event_type="snapshot", side="bid", level=0,
            price=Decimal("100000000"), quantity=0.5,  # float — should raise
        )


# ─── D1: OrderbookSnapshotRecord float 거부 ──────────────────────────────────

def test_orderbook_snapshot_record_rejects_float_price() -> None:
    with pytest.raises(TypeError, match="float"):
        OrderbookSnapshotRecord(
            ts_utc=_TS, received_at=_TS,
            exchange="bithumb", symbol="KRW-BTC",
            baseline_seq=1234567890,
            side="bid", level=0,
            price=99.9,  # float — should raise
            quantity=Decimal("10.0"),
            payload_hash="abc123",
        )


def test_orderbook_snapshot_record_rejects_float_quantity() -> None:
    with pytest.raises(TypeError, match="float"):
        OrderbookSnapshotRecord(
            ts_utc=_TS, received_at=_TS,
            exchange="bithumb", symbol="KRW-BTC",
            baseline_seq=1234567890,
            side="bid", level=0,
            price=Decimal("99000000"), quantity=9.9,  # float — should raise
            payload_hash="abc123",
        )


# ─── D1: ExchangeMetadataRecord float 거부 ───────────────────────────────────

def test_exchange_metadata_record_rejects_float_acc_trade_value() -> None:
    with pytest.raises(TypeError, match="float"):
        ExchangeMetadataRecord(
            exchange="bithumb", symbol="KRW-BTC",
            fetched_date=date(2026, 5, 1),
            fetched_at=_TS,
            source_snapshot_id="abc123",
            data_hash="def456",
            asset_status="1",
            acc_trade_value_24h=12345.6,  # float — should raise
        )


def test_exchange_metadata_record_rejects_float_tick_size() -> None:
    with pytest.raises(TypeError, match="float"):
        ExchangeMetadataRecord(
            exchange="bithumb", symbol="KRW-BTC",
            fetched_date=date(2026, 5, 1),
            fetched_at=_TS,
            source_snapshot_id="abc123",
            data_hash="def456",
            asset_status="1",
            acc_trade_value_24h=Decimal("12345000000"),
            tick_size=1.0,  # float — should raise
        )


def test_exchange_metadata_record_accepts_none_optionals() -> None:
    r = ExchangeMetadataRecord(
        exchange="bithumb", symbol="KRW-BTC",
        fetched_date=date(2026, 5, 1),
        fetched_at=_TS,
        source_snapshot_id="abc123",
        data_hash="def456",
        asset_status="1",
        acc_trade_value_24h=Decimal("12345000000"),
    )
    assert r.tick_size is None
    assert r.acc_trade_value_24h == Decimal("12345000000")
```

- [ ] **Step 2: Run tests to verify they fail**

```
cd c:\workspace\mclayer\mctrader-data
.venv\Scripts\python.exe -m pytest tests/test_adr018_d1_d3.py -v --tb=short -k "d1 or tick_record or orderbook_event or orderbook_snapshot or exchange_metadata"
```

Expected: FAIL (TypeError not raised — no `__post_init__` yet)

- [ ] **Step 3: Add `__post_init__` to TickRecord (`tick_storage.py:51-59`)**

`TickRecord` 정의 끝에 `__post_init__` 추가:

```python
@dataclass
class TickRecord:
    ts_utc: datetime
    received_at: datetime
    exchange: str
    symbol: str
    price: Decimal
    quantity: Decimal
    side: str
    raw_json: str | None = None

    def __post_init__(self) -> None:
        if isinstance(self.price, float):
            raise TypeError("float not allowed for price; use Decimal or str")
        if isinstance(self.quantity, float):
            raise TypeError("float not allowed for quantity; use Decimal or str")
```

- [ ] **Step 4: Add `__post_init__` to OrderbookEventRecord (`orderbook_storage.py:57-68`)**

```python
@dataclass
class OrderbookEventRecord:
    ts_utc: datetime
    received_at: datetime
    exchange: str
    symbol: str
    event_type: str
    side: str
    level: int
    price: Decimal
    quantity: Decimal
    raw_json: str | None = None

    def __post_init__(self) -> None:
        if isinstance(self.price, float):
            raise TypeError("float not allowed for price; use Decimal or str")
        if isinstance(self.quantity, float):
            raise TypeError("float not allowed for quantity; use Decimal or str")
```

- [ ] **Step 5: Add `__post_init__` to OrderbookSnapshotRecord**

`orderbook_snapshot_storage.py` 에서 `OrderbookSnapshotRecord` dataclass 찾아 `__post_init__` 추가:

```python
    def __post_init__(self) -> None:
        if isinstance(self.price, float):
            raise TypeError("float not allowed for price; use Decimal or str")
        if isinstance(self.quantity, float):
            raise TypeError("float not allowed for quantity; use Decimal or str")
```

- [ ] **Step 6: Add `__post_init__` to ExchangeMetadataRecord**

`metadata_storage.py` 에서 `ExchangeMetadataRecord` dataclass 찾아 `__post_init__` 추가:

```python
    def __post_init__(self) -> None:
        _decimal_fields = (
            ("acc_trade_value_24h", self.acc_trade_value_24h),
            ("tick_size", self.tick_size),
            ("min_order_qty", self.min_order_qty),
            ("fee_maker", self.fee_maker),
            ("fee_taker", self.fee_taker),
            ("min_order_notional_krw", self.min_order_notional_krw),
        )
        for name, val in _decimal_fields:
            if isinstance(val, float):
                raise TypeError(f"float not allowed for {name}; use Decimal or str")
```

- [ ] **Step 7: Run D1 tests to verify GREEN**

```
.venv\Scripts\python.exe -m pytest tests/test_adr018_d1_d3.py -v --tb=short
```

Expected: D1 관련 tests 모두 PASS

- [ ] **Step 8: Run full suite to confirm no regressions**

```
.venv\Scripts\python.exe -m pytest tests/ -q --tb=short
```

Expected: 280+ passed (기존 tests 모두 통과)

- [ ] **Step 9: Commit**

```
git add src/mctrader_data/tick_storage.py \
        src/mctrader_data/orderbook_storage.py \
        src/mctrader_data/orderbook_snapshot_storage.py \
        src/mctrader_data/metadata_storage.py \
        tests/test_adr018_d1_d3.py
git commit -m "fix(adr018-d1): add float rejection __post_init__ to 4 dataclasses (MCT-115)"
```

---

## Task 2: D2 — CollectorManifest frozen=True + tuple 전환

**Files:**
- Modify: `src/mctrader_data/manifest.py:20-40` (`CollectorManifest`)
- Test: `tests/test_adr018_d1_d3.py` (D2 tests 추가)

- [ ] **Step 1: Write failing tests (D2 섹션을 test_adr018_d1_d3.py에 추가)**

`tests/test_adr018_d1_d3.py` 하단에 추가:

```python
# ─── D2: CollectorManifest frozen + tuple ─────────────────────────────────────

from mctrader_data.manifest import CollectorManifest


def test_collector_manifest_frozen() -> None:
    m = CollectorManifest(
        collector_run_id="run-001",
        started_at_utc=_TS,
        exchange="bithumb",
        selected_symbols=["KRW-BTC", "KRW-ETH"],
        channels=["transaction"],
        selection_method="explicit",
    )
    with pytest.raises(Exception):
        m.collector_run_id = "tampered"  # type: ignore[misc]


def test_collector_manifest_selected_symbols_is_tuple() -> None:
    m = CollectorManifest(
        collector_run_id="run-001",
        started_at_utc=_TS,
        exchange="bithumb",
        selected_symbols=["KRW-BTC", "KRW-ETH"],  # list input — should coerce
        channels=["transaction"],
        selection_method="explicit",
    )
    assert isinstance(m.selected_symbols, tuple)
    assert m.selected_symbols == ("KRW-BTC", "KRW-ETH")


def test_collector_manifest_channels_is_tuple() -> None:
    m = CollectorManifest(
        collector_run_id="run-001",
        started_at_utc=_TS,
        exchange="bithumb",
        selected_symbols=["KRW-BTC"],
        channels=["transaction", "orderbook"],  # list input — should coerce
        selection_method="explicit",
    )
    assert isinstance(m.channels, tuple)
    assert m.channels == ("transaction", "orderbook")


def test_collector_manifest_roundtrip_json_preserves_tuple() -> None:
    m = CollectorManifest(
        collector_run_id="run-001",
        started_at_utc=_TS,
        exchange="bithumb",
        selected_symbols=["KRW-BTC"],
        channels=["transaction"],
        selection_method="explicit",
    )
    json_str = m.model_dump_json()
    m2 = CollectorManifest.model_validate_json(json_str)
    assert isinstance(m2.selected_symbols, tuple)
    assert isinstance(m2.channels, tuple)
```

- [ ] **Step 2: Run D2 tests to verify they fail**

```
.venv\Scripts\python.exe -m pytest tests/test_adr018_d1_d3.py::test_collector_manifest_frozen tests/test_adr018_d1_d3.py::test_collector_manifest_selected_symbols_is_tuple tests/test_adr018_d1_d3.py::test_collector_manifest_channels_is_tuple tests/test_adr018_d1_d3.py::test_collector_manifest_roundtrip_json_preserves_tuple -v --tb=short
```

Expected: FAIL (fields are still `list[str]`, not tuple; no frozen)

- [ ] **Step 3: Update CollectorManifest in manifest.py**

`manifest.py` 상단 import 에 `Annotated` 추가 후 `CollectorManifest` 클래스 수정:

```python
from typing import Annotated, Literal  # Annotated 추가

from pydantic import BaseModel, BeforeValidator, ConfigDict, Field
```

`CollectorManifest.model_config` 변경:
```python
model_config = ConfigDict(strict=True, extra="forbid", frozen=True)
```

`selected_symbols`, `channels` 필드 변경:
```python
selected_symbols: Annotated[tuple[str, ...], BeforeValidator(tuple)]
channels: Annotated[tuple[str, ...], BeforeValidator(tuple)]
```

전체 변경 후 클래스:
```python
class CollectorManifest(BaseModel):
    """Per-collector-run metadata. Pydantic v2 strict, file = JSON.

    MCT-91 (Phase 2) — `node_id` field 추가 (optional, default None).
    Legacy manifest (pre-HA) 는 `node_id` field 없음 → backward compat read 보장.
    ADR-018 D2: frozen=True; selected_symbols/channels → tuple (immutable after creation).
    """

    model_config = ConfigDict(strict=True, extra="forbid", frozen=True)

    schema_version: Literal["collector_manifest.v1"] = "collector_manifest.v1"
    collector_run_id: str
    started_at_utc: datetime
    exchange: str
    selected_symbols: Annotated[tuple[str, ...], BeforeValidator(tuple)]
    channels: Annotated[tuple[str, ...], BeforeValidator(tuple)]
    selection_method: Literal["explicit", "top_n_volume"]
    top_n: int | None = Field(default=None, description="present iff selection_method=top_n_volume")
    node_id: str | None = Field(
        default=None,
        description="HA node identifier (MCT-91). None = pre-HA legacy manifest.",
    )
```

- [ ] **Step 4: Run D2 tests GREEN**

```
.venv\Scripts\python.exe -m pytest tests/test_adr018_d1_d3.py -v --tb=short
```

Expected: D2 관련 tests 모두 PASS

- [ ] **Step 5: Run full suite to confirm no regressions**

```
.venv\Scripts\python.exe -m pytest tests/ -q --tb=short
```

Expected: 모든 tests PASS (`test_collector_manifest.py` 포함)

- [ ] **Step 6: Commit**

```
git add src/mctrader_data/manifest.py tests/test_adr018_d1_d3.py
git commit -m "fix(adr018-d2): CollectorManifest frozen=True + tuple fields (MCT-115)"
```

---

## Task 3: D3 — OhlcvRow low ≤ high cross-field validator

**Files:**
- Modify: `src/mctrader_data/schema.py` (`OhlcvRow`)
- Test: `tests/test_adr018_d1_d3.py` (D3 tests 추가)

- [ ] **Step 1: Write failing tests (D3 섹션 추가)**

`tests/test_adr018_d1_d3.py` 하단에 추가:

```python
# ─── D3: OhlcvRow cross-field invariant ──────────────────────────────────────

from pydantic import ValidationError as PydanticValidationError
from mctrader_market.types import Symbol, Timeframe
from mctrader_data.schema import OhlcvRow


_SYM = Symbol(base="BTC", quote="KRW")


def test_ohlcv_row_rejects_low_gt_high() -> None:
    with pytest.raises(PydanticValidationError, match="low.*high|OHLCV"):
        OhlcvRow(
            ts_utc=_TS, exchange="bithumb",
            symbol=_SYM, timeframe=Timeframe.H1,
            open=Decimal("100000000"),
            high=Decimal("99000000"),   # high < low → invalid
            low=Decimal("100500000"),
            close=Decimal("100200000"),
            volume=Decimal("1.5"),
        )


def test_ohlcv_row_rejects_low_gt_open() -> None:
    with pytest.raises(PydanticValidationError, match="low.*open|OHLCV"):
        OhlcvRow(
            ts_utc=_TS, exchange="bithumb",
            symbol=_SYM, timeframe=Timeframe.H1,
            open=Decimal("98000000"),   # open < low → invalid
            high=Decimal("101000000"),
            low=Decimal("99000000"),
            close=Decimal("100000000"),
            volume=Decimal("1.5"),
        )


def test_ohlcv_row_rejects_close_gt_high() -> None:
    with pytest.raises(PydanticValidationError, match="close.*high|OHLCV"):
        OhlcvRow(
            ts_utc=_TS, exchange="bithumb",
            symbol=_SYM, timeframe=Timeframe.H1,
            open=Decimal("100000000"),
            high=Decimal("100500000"),
            low=Decimal("99500000"),
            close=Decimal("101000000"),  # close > high → invalid
            volume=Decimal("1.5"),
        )


def test_ohlcv_row_rejects_negative_volume() -> None:
    with pytest.raises(PydanticValidationError, match="volume"):
        OhlcvRow(
            ts_utc=_TS, exchange="bithumb",
            symbol=_SYM, timeframe=Timeframe.H1,
            open=Decimal("100000000"),
            high=Decimal("100500000"),
            low=Decimal("99500000"),
            close=Decimal("100200000"),
            volume=Decimal("-1"),  # negative volume → invalid
        )


def test_ohlcv_row_accepts_valid_candle() -> None:
    row = OhlcvRow(
        ts_utc=_TS, exchange="bithumb",
        symbol=_SYM, timeframe=Timeframe.H1,
        open=Decimal("100000000"),
        high=Decimal("100500000"),
        low=Decimal("99500000"),
        close=Decimal("100200000"),
        volume=Decimal("1.5"),
    )
    assert row.high >= row.low


def test_ohlcv_row_accepts_doji_candle() -> None:
    """open == close == high == low (점형 봉) 는 유효."""
    v = Decimal("100000000")
    row = OhlcvRow(
        ts_utc=_TS, exchange="bithumb",
        symbol=_SYM, timeframe=Timeframe.H1,
        open=v, high=v, low=v, close=v,
        volume=Decimal("0"),
    )
    assert row.open == row.close
```

- [ ] **Step 2: Run D3 tests to verify they fail**

```
.venv\Scripts\python.exe -m pytest tests/test_adr018_d1_d3.py -v --tb=short -k "ohlcv"
```

Expected: FAIL (ValidationError not raised — no model_validator yet)

- [ ] **Step 3: Add model_validator to OhlcvRow (schema.py)**

`schema.py` import 에 `model_validator` 추가:

```python
from pydantic import BaseModel, ConfigDict, model_validator
```

`OhlcvRow` 클래스에 validator 추가 (`data_hash` 필드 뒤):

```python
    @model_validator(mode="after")
    def _check_ohlcv_invariants(self) -> "OhlcvRow":
        if self.low > self.high:
            raise ValueError(
                f"OHLCV invariant violated: low ({self.low}) > high ({self.high})"
            )
        if self.open < self.low or self.open > self.high:
            raise ValueError(
                f"OHLCV invariant violated: low <= open <= high required "
                f"(low={self.low}, open={self.open}, high={self.high})"
            )
        if self.close < self.low or self.close > self.high:
            raise ValueError(
                f"OHLCV invariant violated: low <= close <= high required "
                f"(low={self.low}, close={self.close}, high={self.high})"
            )
        if self.volume < Decimal(0):
            raise ValueError(f"OHLCV invariant violated: volume must be >= 0, got {self.volume}")
        return self
```

`schema.py` 상단 import에 `Decimal` 추가:

```python
from decimal import Decimal
```

- [ ] **Step 4: Run D3 tests GREEN**

```
.venv\Scripts\python.exe -m pytest tests/test_adr018_d1_d3.py -v --tb=short -k "ohlcv"
```

Expected: D3 관련 tests 모두 PASS

- [ ] **Step 5: Run full suite (schema tests 포함 regression check)**

```
.venv\Scripts\python.exe -m pytest tests/ -q --tb=short
```

Expected: 모든 tests PASS (`tests/test_schema.py` 포함)

- [ ] **Step 6: Commit**

```
git add src/mctrader_data/schema.py tests/test_adr018_d1_d3.py
git commit -m "fix(adr018-d3): OhlcvRow model_validator low<=open,close<=high invariant (MCT-115)"
```

---

## Task 4: D5 — WalIngester O_APPEND 패턴 주석 명시

**Files:**
- Modify: `src/mctrader_data/wal/ingester.py:51-61` (`append()` 메서드)

- [ ] **Step 1: WalIngester.append() 메서드에 WHY 주석 추가**

`append()` 메서드 docstring / 인라인 주석 추가:

```python
    def append(self, record: dict) -> None:
        # ADR-018 D5: O_APPEND+fsync 패턴 (tmp-rename 불필요한 이유):
        #   1) 각 NDJSON 라인은 완결된 레코드 — 부분 라인은 없음 (encode_record 보장).
        #   2) threading.Lock이 단일 writer 직렬화 → POSIX O_APPEND 원자성 충분.
        #   3) tmp-rename 은 세그먼트 봉인(seal) 시에만 사용 (_seal_current/close).
        if self._closed:
            raise RuntimeError("WalIngester is closed")
        with self._lock:
            self.maybe_seal()
            line = encode_record(record).encode("utf-8")
            assert self._fd is not None
            os.write(self._fd, line)
            self._write_count += 1
            if self._write_count % self._fsync_batch == 0:
                os.fsync(self._fd)
```

- [ ] **Step 2: Run WAL tests to confirm no regression**

```
.venv\Scripts\python.exe -m pytest tests/test_wal_ingester.py tests/test_wal_ingester_crash_recovery.py tests/test_wal_ndjson_codec.py -v --tb=short
```

Expected: 모두 PASS

- [ ] **Step 3: Commit**

```
git add src/mctrader_data/wal/ingester.py
git commit -m "docs(adr018-d5): explain O_APPEND vs tmp-rename pattern in WalIngester.append (MCT-115)"
```

---

## Task 5: 전체 회귀 검증 + PR 생성

- [ ] **Step 1: 전체 pytest GREEN 확인**

```
.venv\Scripts\python.exe -m pytest tests/ -v --tb=short
```

Expected: 모든 tests PASS (280+ tests, 신규 tests 포함)

- [ ] **Step 2: 브랜치 확인**

```
git branch --show-current
```

Expected: `feat/mct-115-adr018-audit-data`

- [ ] **Step 3: PR 생성**

```
gh pr create \
  --title "fix(mct-115): ADR-018 D1/D2/D3 audit fixes + D5 comment" \
  --body "..."
```

PR body:
```
## Summary

- D1: 4개 dataclass (`TickRecord`, `OrderbookEventRecord`, `OrderbookSnapshotRecord`, `ExchangeMetadataRecord`) float rejection `__post_init__` 추가
- D2: `CollectorManifest` `frozen=True` + `selected_symbols`/`channels` → `tuple[str, ...]` (BeforeValidator coerce)
- D3: `OhlcvRow` `@model_validator(mode="after")` — OHLCV 불변식 (`low <= open, close <= high`, `volume >= 0`)
- D5: `WalIngester.append()` O_APPEND vs tmp-rename 선택 근거 주석
- D4/D6/D7: N/A (사유 명시)

## N/A 사유

- **D4**: `CompactorRunner` asyncio 단일 스레드 — `_last_l2/_last_l3` 이벤트 루프 내 단독 접근, TOCTOU 없음. `WalIngester` `threading.Lock` ✓.
- **D6**: `health_server.py` GET-only, 보안 관련 request header 없음. `httpx.Headers` case-insensitive by design.
- **D7**: CLI 전체 `--skip-validation`/`--bypass-*` flag 없음 확인.

## Test plan

- [ ] `pytest tests/test_adr018_d1_d3.py -v` → 신규 D1/D2/D3 테스트 PASS
- [ ] `pytest tests/ -q` → 280+ 전체 tests GREEN
```

---

## N/A 상세 근거

### D4 (threading.Lock / TOCTOU)

`CompactorRunner.run()` → `_tick()`:
- asyncio 이벤트 루프 단일 스레드 실행 → `_last_l2`, `_last_l3` TOCTOU 없음
- `run_in_executor`는 L2/L3를 스레드 풀에서 실행하지만, `_last_l2 = now` 갱신은 executor 호출 **전** 이벤트 루프에서 완료됨
- `WalIngester`: `threading.Lock` 이미 적용 ✓
- 결론: **D4 위반 없음**

### D6 (case-insensitive header guard)

- `health_server.py` (`stdlib http.server`): GET /health 응답 전용, request header 파싱 없음. `Authorization` 류 보안 헤더 미사용.
- `metadata_storage.py` (`httpx` 클라이언트): `httpx.Headers`는 case-insensitive 구현 (`MutableHeaders` with lowercase keys). 명시적 헤더 접근 없음.
- 결론: **D6 N/A**

### D7 (governance flag)

CLI 전체 옵션 점검 결과:
- `backfill`: `--dry-run` 있음 (preview, 실제 write 없음) — bypass 아님
- `collect`, `compact`, `status`: governance bypass 없음
- 결론: **D7 위반 없음**
